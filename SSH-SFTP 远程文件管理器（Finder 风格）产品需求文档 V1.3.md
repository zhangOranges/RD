# SSH-SFTP远程文件管理器（Finder风格）产品需求文档 V1.3

**更新说明**：本次V1.3版本移除系统级虚拟磁盘挂载相关规划，全程坚持无内核驱动的纯SFTP轻量化方案；新增系统设置中心、主机分组管理、基础传输队列、断线重连策略、首次启动引导等模块，补全配置体系与落地细节，优化文档结构完整性与可执行性；原有核心功能全部保留。

**开发技术栈**：Tauri2.0 + Rust(russh) + React + Xterm.js

**适配平台**：Windows、macOS、Linux 全平台

**产品定位**：复刻macOS Finder访达UI与交互、单SSH会话复用终端+文件通道、目录双向联动的一体化Linux远程管理客户端，实现媲美本地文件夹的远程操作体验。

## 一、项目概述
### 1.1 项目简介
本软件是一站式Linux远程管理工具，完全对标macOS Finder访达简约视觉与交互逻辑。基于单条SSH长连接，同时承载PTY交互式终端、SFTP文件管理双通道，实现文件可视化管理、内置终端联动、地址栏快捷唤起Shell、远程文件惰性缓存编辑、目录记忆恢复等核心能力。全程无系统驱动依赖，跨端适配，轻量化、高性能、无臃肿冗余。

### 1.2 核心开发目标
- UI 1:1对标macOS Finder，支持分栏、列表、图标三种浏览视图
- 单SSH长连接复用PTY终端通道+SFTP文件通道，目录状态完全互通
- 顶部地址栏输入cmd/terminal一键唤起内置SSH终端
- 终端与文件浏览器双向实时目录同步，无状态偏差
- 支持连接自动记忆上次浏览目录，可配置、默认开启
- 惰性缓存方案实现远程文件本地编辑、自动回传、冲突检测，无内核驱动依赖
- 多服务器多标签会话管理、完整SFTP文件操作、主题适配

### 1.3 明确非目标
- 不提供系统级虚拟磁盘挂载能力，全程基于SFTP协议实现文件管理，无任何内核驱动依赖
- 不劫持系统原生终端（PowerShell、iTerm、系统终端），所有远程命令仅在内置终端生效
- 一期不支持跳板机代理、端口转发、大文件断点续传、全局文件内容搜索

## 二、整体功能架构
整体分为四大核心层级，完全解耦、分层开发：
1. **Rust底层通信层**：基于russh实现SSH长连接管理、PTY终端通道、SFTP协议通信、断线自动重连、文件传输队列、路径缓存读写、异常捕获降级
2. **前端UI交互层**：React实现Finder风格界面、三栏分栏浏览、地址栏、文件列表、视图切换、主机侧边栏
3. **终端联动层**：Xterm.js实现内置交互式终端，完成双向目录同步、命令转发、路径校验
4. **本地持久化层**：系统密钥链加密存储主机凭据，本地缓存存储会话路径、主机配置、软件个性化设置

## 三、详细功能性需求
### 3.1 主机连接 & 会话管理模块
#### 3.1.1 侧边栏设计（复刻Finder）
- 侧边栏分为三大分组：已保存主机、收藏远程目录、快捷入口
- 「已保存主机」支持创建自定义分组，支持主机拖拽移入分组、分组折叠展开；分组支持重命名、删除、手动排序，删除分组不删除分组内主机
- 支持主机自定义名称、IP、SSH端口、账号登录、密码/Ed25519/RSA私钥双登录方式
- 主机右键菜单：新增、编辑、删除、复制主机地址、打开远程根目录、移动至分组
- **主机独立可配置项**：【连接成功自动跳转上次浏览目录】，**默认开启**，支持单主机单独关闭，优先级高于全局设置
- 收藏目录支持两种添加方式：目录右键「添加到收藏」、拖拽目录至侧边栏收藏分组；收藏项支持重命名、删除、手动拖拽排序，点击直接跳转对应远程目录

#### 3.1.2 SSH会话硬性核心规则
- 每台远程服务器仅维持**一条SSH长连接**，禁止多连接拆分终端与文件服务
- 单连接并行创建两条独立子通道：PTY交互式Shell通道、SFTP文件传输通道，会话状态完全互通
- **双通道生命周期规则**：SSH主连接建立后优先创建SFTP通道并全程常驻；PTY通道按需创建，终端面板唤起时建立、收起时销毁，终端关闭不中断SSH主连接与SFTP服务；单通道异常时独立重建，不影响另一通道正常运行
- **目录记忆初始化规则**：连接成功后，读取当前主机当前标签的历史缓存路径；配置开启时，自动在PTY执行cd切换至上次目录，同步刷新文件浏览器；配置关闭时，默认进入用户家目录 ~
- **失效降级规则**：若缓存的历史目录已被服务器删除、路径不存在，自动降级进入用户家目录，并前端轻量弹窗提示
- 支持网络波动断线自动重连，采用指数退避策略，最大重连5次；连续失败后停止自动重连，提示手动重连；重连成功后自动恢复当前目录、终端会话与进行中的传输队列

#### 3.1.3 多标签会话管理
- 支持多服务器独立标签页，每个标签对应独立SSH会话、独立路径缓存
- 不同主机、不同标签的历史浏览路径互相隔离，互不干扰
- 支持标签拖拽排序、关闭其他标签、关闭右侧标签、复制标签等右键操作

#### 3.1.4 路径持久化规则
- 实时监听目录切换动作（文件浏览切换、终端cd切换）
- 每次目录变更后，自动覆盖更新当前标签、当前主机的最新绝对路径缓存
- 关闭软件、断开连接、重启程序，路径缓存永久保留，下次连接自动复用

#### 3.1.5 主机配置与持久化字段
- 主机核心配置字段：主机唯一ID、所属分组ID、自定义名称、主机IP、SSH端口、登录用户名、认证类型（密码/私钥）、目录记忆开关、备注信息
- 敏感凭据（密码、私钥内容）统一存入系统密钥链加密存储，禁止明文写入本地配置文件
- 非敏感配置与路径缓存存储于Tauri应用数据目录，采用结构化JSON格式管理；路径缓存唯一键规则为「主机ID+标签ID」，实现多标签路径隔离
- 支持主机配置的导入、导出，导出文件自动脱敏，不含密码与私钥内容；支持批量导入主机配置

### 3.2 Finder风格文件浏览模块
#### 3.2.1 顶部工具栏
- 基础操作：前进、后退、返回上级目录、新建文件夹、删除、刷新
- 核心组件：地址栏默认展示分级面包屑，每一级目录均可点击跳转；点击/双击地址栏切换为纯路径输入模式，支持直接编辑路径；右键地址栏可复制当前路径、在终端打开当前目录
- 功能按钮：三种视图切换、当前目录文件名搜索

#### 3.2.2 三大浏览视图（完整复刻Finder）
- **分栏视图（默认）**：三栏联动布局，上级目录、子目录、当前目录文件，支持拖拽调整栏宽，macOS原生交互
- **列表视图**：表格展示文件名称、修改日期、文件大小、权限、所属用户、文件类型，支持按多维度升序/降序排序
- **图标视图**：网格大图标展示文件/文件夹，适配macOS简约视觉风格，支持图标大小调节

#### 3.2.3 目录基础交互
- 双击文件夹进入下级目录，平滑刷新页面
- 地址栏输入合法Linux绝对路径，回车一键跳转对应远程目录
- 支持鼠标侧键前进/后退目录，贴合系统原生操作习惯

#### 3.2.4 全局快捷键体系
对标Finder统一键位规范，同时适配Windows与macOS平台差异，支持后续自定义扩展：
- 新建标签页：Ctrl+T（Windows）/ Cmd+T（macOS）
- 关闭当前标签：Ctrl+W（Windows）/ Cmd+W（macOS）
- 刷新当前目录：F5 / Ctrl+R（Windows）/ Cmd+R（macOS）
- 唤起/收起终端：Ctrl+`（Windows）/ Cmd+`（macOS）
- 返回上级目录：Backspace（Windows）/ Command+↑（macOS）
- 删除选中文件：Delete（Windows）/ Cmd+Backspace（macOS）
- 新建主机连接：Ctrl+N（Windows）/ Cmd+N（macOS）
- 打开设置：Ctrl+,（Windows）/ Cmd+,（macOS）

#### 3.2.5 界面布局状态记忆
- 按主机维度持久化保存界面布局状态，下次连接自动恢复
- 记忆内容包含：侧边栏展开/收起状态、侧边栏宽度、终端面板高度、当前视图模式、分栏视图各栏宽度、列表视图排序列与排序方向
- 布局变更实时写入本地缓存，软件重启不丢失

### 3.3 地址栏专属核心交互
- 输入合法远程路径：触发SFTP目录跳转，刷新文件列表
- 输入 **cmd / terminal** 回车：底部滑出内置Xterm终端面板，自动cd至当前浏览远程路径
- 进阶能力：地址栏直接输入Linux命令，回车自动投递至终端执行
- **路径合法性校验规则**：输入相对路径时自动补全为基于当前目录的绝对路径；输入非法路径时地址栏标红提示，不执行跳转；路径中包含空格、特殊字符时自动做转义处理

### 3.4 终端 & 文件浏览器双向同步（硬性核心）
**核心约束**：禁止解析用户输入的cd命令字符串，统一通过服务端执行pwd获取服务器真实绝对路径，规避软链接、相对路径、用户别名导致的路径错乱问题
- **文件浏览器→终端同步**：前端切换远程目录后，后端通过PTY通道下发cd指令，同步终端工作目录
- **终端→文件浏览器同步**：每条终端命令执行完毕，后端自动执行pwd获取真实路径，推送前端刷新文件浏览界面与地址栏

### 3.5 SFTP完整文件操作能力
- 基础操作：新建文件夹、重命名、删除、复制、剪切、粘贴远程文件/目录
- 批量操作：支持框选、Ctrl多选批量文件/目录，支持批量删除、批量拖拽上传下载；批量删除前弹出二次确认；批量操作出现部分成功、部分失败时，统一展示失败列表与原因
- 拖拽传输：本地文件拖拽上传至远程服务器、远程文件拖拽下载至本地电脑
- 同名文件处理：上传/下载遇到同名文件时，默认弹出选项面板，支持覆盖、跳过、重命名三种处理方式；可在设置中配置默认处理逻辑
- 基础传输队列：同时支持多个文件排队传输，底部状态栏展示当前传输进度、速度、剩余文件数；支持手动暂停、取消单个传输任务，取消后自动清理不完整文件
- 右键菜单：修改文件chmod权限、复制远程路径、在终端打开当前目录、添加到收藏
- 快速预览：支持txt、json、图片等常见格式文件内置预览，无需完整下载

### 3.6 惰性缓存文件编辑方案（MVP核心）
无任何系统驱动依赖，全平台开箱即用，规避杀毒拦截、系统权限问题
- 双击远程文件：SFTP惰性下载文件至系统临时目录，唤起本地默认编辑器打开
- 改动监听：实时监听本地临时文件变更，文件保存后触发回传
- 冲突防护：上传前读取远程文件mtime时间戳，远端文件已变更则弹窗冲突警告，禁止强制覆盖
- 自动回传：无冲突时，SFTP自动上传修改后文件覆盖远程服务端文件
- 缓存清理：支持配置软件退出自动清空所有临时缓存文件
- 传输中断处理：手动取消或网络中断时，自动清理不完整的临时文件，不残留垃圾文件

### 3.7 内置终端模块能力
- 完整Linux终端交互，支持彩色输出、复制粘贴、快捷键操作
- 终端面板支持展开/收起、拖拽调整高度、清屏、字体大小调节
- 终端操作不阻塞文件浏览、文件传输功能，后台异步运行
- PTY通道异常断开时，终端展示「连接已断开，点击重试」，支持手动重启终端会话，无需重建SSH主连接
- 支持终端分屏（二期拓展），单标签页下可开启多个终端会话

### 3.8 系统适配与个性化
- 适配系统浅色/深色模式，支持跟随系统、强制浅色、强制深色三种模式
- 全局圆角控件、柔和分割线、低饱和配色，统一苹果UI质感
- 适配Windows/macOS/Linux原生窗口特性与系统缩放

### 3.9 异常与边界处理
- **权限不足场景**：目录无读权限时，列表区域展示「无访问权限」占位提示，不崩溃报错；文件无读权限时，预览/编辑操作弹出权限不足提示；无写权限时，上传、重命名、删除操作前置置灰，点击后反馈明确原因
- **目录不存在场景**：跳转目标目录不存在时，轻量提示路径无效，停留在当前目录，不强制跳转
- **文件传输异常**：传输过程中网络中断时，自动终止传输并清理不完整文件；重连后不自动续传，需用户手动重新发起
- **统一反馈规范**：轻量操作成功采用顶部Toast提示；异常警告、确认类操作采用模态弹窗；连接状态、传输进度采用底部状态栏常驻展示

### 3.10 系统设置中心
#### 3.10.1 通用设置
- 目录记忆全局开关：默认开启，关闭后所有主机默认进入用户家目录；单主机独立配置优先级高于全局
- 软件退出自动清理临时缓存：开关项，默认关闭
- 新建标签默认路径：可配置为用户家目录或上次浏览路径
- 断线自动重连：开关项，默认开启，最大重连5次，指数退避策略

#### 3.10.2 外观设置
- 主题模式：跟随系统/浅色/深色三选一
- 终端字体、字号、行高自定义
- 侧边栏图标大小、列表视图行高调节

#### 3.10.3 传输设置
- 同名文件默认处理方式：询问/覆盖/跳过/自动重命名
- 并发传输数限制：默认同时传输3个文件
- 下载默认保存路径配置

#### 3.10.4 安全与日志
- 服务器指纹校验策略：首次信任/严格校验
- 日志等级配置：INFO/WARN/ERROR三级
- 一键导出脱敏日志包
- 一键清除所有本地缓存与配置

### 3.11 首次启动引导
- 首次启动软件，分步引导用户添加第一台主机，支持快速导入现有SSH配置
- 核心功能引导：演示地址栏唤终端、目录双向同步、文件拖拽上传三个核心操作
- 引导用户选择主题模式、配置基础安全选项

## 四、非功能性需求
### 4.1 性能需求
- 大文件目录启用虚拟滚动渲染，上千文件列表滚动无明显卡顿
- SSH长连接常驻，减少重复握手开销，常规目录切换响应≤300ms
- 文件传输异步后台执行，不阻塞UI渲染与终端交互
- 软件冷启动时间≤2秒，内存占用控制在合理范围

### 4.2 安全需求
- SSH服务器指纹校验，抵御中间人攻击
- 主机密码、私钥通过系统密钥链加密存储，禁止明文保存
- 所有SFTP、终端通信全程基于SSH加密通道传输
- 本地日志自动脱敏，不记录密码、私钥、敏感命令内容

### 4.3 兼容性需求
- 兼容OpenSSH新版Ed25519、RSA私钥格式
- 兼容CentOS、Ubuntu、Debian等主流Linux发行版
- 兼容Windows 10+、macOS 11+、主流Linux桌面发行版

### 4.4 可维护性
- 核心模块解耦，通信层与UI层分离，便于后续功能扩展
- 完善的错误码体系与日志分级，便于问题定位排查
- 支持配置热更新，基础设置修改无需重启软件

## 五、功能迭代优先级
### P0 必做MVP（第一期上线）
1. Russh实现单SSH连接同时启用PTY、SFTP双通道
2. 复刻macOS Finder完整UI：侧边栏、地址栏面包屑、三栏分栏视图
3. 实现主机「自动跳转上次目录」可配置功能+路径持久化缓存
4. 地址栏输入cmd唤起内置终端面板
5. 终端与文件浏览器双向目录同步
6. 基础SFTP目录浏览、新建、删除、重命名操作
7. 基础主机管理与系统设置核心项

**P0验收标准**：单SSH连接双通道稳定运行2小时无中断；千级文件目录滚动无明显卡顿；目录双向同步延迟≤200ms；核心操作无崩溃、无路径状态不一致问题；密码私钥全程加密存储。

### P1 一期完善功能
1. 惰性缓存文件编辑、mtime冲突检测、自动上传回传
2. 本地↔远程文件拖拽上传下载、基础传输队列
3. 多标签会话管理、断线自动重连
4. 全局快捷键、布局状态记忆、收藏目录功能
5. 主机分组管理、配置导入导出

**P1验收标准**：文件编辑保存后1秒内自动回传服务端；拖拽上传下载支持单文件最大1GB；断线30秒内自动重连并恢复会话状态；快捷键全场景生效无冲突。

### P2 二期拓展功能
1. 列表/图标视图切换、文件chmod权限设置
2. 文件内置预览、全局深浅色主题适配
3. 批量文件操作、全场景异常边界处理
4. 终端分屏、完整设置中心、首次启动引导

**P2验收标准**：三种视图切换流畅无白屏；权限修改实时生效；批量操作成功率≥99%；深浅主题无样式错乱；引导流程可完整走通。

### P3 三期高级功能
1. 跳板机代理、端口转发功能
2. 文件断点续传、完整传输队列管理
3. 远程文件全局内容搜索
4. 终端会话保存与快速恢复

**P3验收标准**：支持多级跳板机连接；断点续传后文件MD5校验一致；全局搜索响应时间≤3秒。