name: Bump Version

# 手动触发：在 Actions 页面运行，选择递增类型（patch / minor / major）。
# 工作流分两步执行：
#   1) 更新版本号 → commit 并 push 到 main → 触发 CI 工作流
#   2) 等待 CI 工作流成功完成后，再 push tag → 触发 Release 工作流
#
# 这样确保 Release 能命中 CI 在 main 上保存的 Rust 编译缓存。
#
# 使用方式：
#   1. GitHub 仓库 → Actions 标签页 → 左侧选择 "Bump Version"
#   2. 点击 "Run workflow" → 选择分支（main）→ 选择 bump 类型 → Run
#   3. 等待该工作流完成（CI 通常 3-5 分钟），release.yml 会被 tag 自动触发

on:
  workflow_dispatch:
    inputs:
      bump:
        description: '版本递增类型'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  actions: read

jobs:
  bump:
    name: Bump & Tag
    runs-on: ubuntu-latest
    outputs:
      next: ${{ steps.ver.outputs.next }}
      tag: ${{ steps.commit-and-tag.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Read current version & compute next
        id: ver
        run: |
          set -euo pipefail
          CUR=$(node -p "require('./package.json').version")
          echo "当前版本: $CUR"
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CUR"
          BUMP="${{ inputs.bump }}"
          case "$BUMP" in
            major) MAJOR=$((MAJOR+1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR+1)); PATCH=0 ;;
            patch) PATCH=$((PATCH+1)) ;;
            *) echo "未知 bump 类型: $BUMP"; exit 1 ;;
          esac
          NEXT="${MAJOR}.${MINOR}.${PATCH}"
          echo "下一版本: $NEXT"
          echo "current=$CUR" >> "$GITHUB_OUTPUT"
          echo "next=$NEXT" >> "$GITHUB_OUTPUT"

      - name: Bump package.json
        run: |
          node -e "
            const fs = require('fs');
            const p = './package.json';
            const j = JSON.parse(fs.readFileSync(p, 'utf8'));
            j.version = '${{ steps.ver.outputs.next }}';
            fs.writeFileSync(p, JSON.stringify(j, null, 2) + '\n');
            console.log('package.json -> ${{ steps.ver.outputs.next }}');
          "

      - name: Bump src-tauri/Cargo.toml
        run: |
          sed -i -E '0,/^version = ".*"/s/^version = ".*"/version = "${{ steps.ver.outputs.next }}"/' src-tauri/Cargo.toml
          grep -m1 '^version' src-tauri/Cargo.toml

      - name: Sync Cargo.lock version
        run: |
          # 只同步 rd-app 的 version 字段到 Cargo.lock，不重新解析依赖
          # 这样 Cargo.lock hash 保持不变（只有 rd-app version 一行变化），
          # CI 和 Release 的缓存 key 仍能命中。
          cd src-tauri
          cargo update -p rd-app --precise "${{ steps.ver.outputs.next }}" 2>/dev/null || true
          # 兜底：直接用 sed 替换 Cargo.lock 中 rd-app 的 version
          if grep -q 'name = "rd-app"' Cargo.lock; then
            sed -i '/name = "rd-app"/,/version = /s/version = "[^"]*"/version = "${{ steps.ver.outputs.next }}"/' Cargo.lock
            echo "Cargo.lock rd-app version 已同步"
          fi

      - name: Bump src-tauri/tauri.conf.json
        run: |
          node -e "
            const fs = require('fs');
            const p = './src-tauri/tauri.conf.json';
            const j = JSON.parse(fs.readFileSync(p, 'utf8'));
            j.version = '${{ steps.ver.outputs.next }}';
            fs.writeFileSync(p, JSON.stringify(j, null, 2) + '\n');
            console.log('tauri.conf.json -> ${{ steps.ver.outputs.next }}');
          "

      - name: Verify version consistency
        run: |
          V_PKG=$(node -p "require('./package.json').version")
          V_TF=$(node -p "require('./src-tauri/tauri.conf.json').version")
          V_CARGO=$(grep -m1 '^version' src-tauri/Cargo.toml | sed -E 's/.*"(.*)".*/\1/')
          V_LOCK=$(grep -A2 'name = "rd-app"' src-tauri/Cargo.lock | grep 'version' | sed -E 's/.*"(.*)".*/\1/')
          echo "package.json:      $V_PKG"
          echo "tauri.conf.json:   $V_TF"
          echo "Cargo.toml:        $V_CARGO"
          echo "Cargo.lock rd-app: $V_LOCK"
          if [ "$V_PKG" != "$V_TF" ] || [ "$V_PKG" != "$V_CARGO" ]; then
            echo "::error::版本号不一致"
            exit 1
          fi
          if [ "$V_PKG" != "$V_LOCK" ]; then
            echo "::error::Cargo.lock 中 rd-app 版本号未同步"
            exit 1
          fi
          if [ "$V_PKG" != "${{ steps.ver.outputs.next }}" ]; then
            echo "::error::版本号未正确更新"
            exit 1
          fi
          echo "版本号一致性校验通过"

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit to main (触发 CI) & 创建 tag（暂不推送）
        id: commit-and-tag
        run: |
          TAG="v${{ steps.ver.outputs.next }}"
          git add package.json src-tauri/Cargo.toml src-tauri/tauri.conf.json src-tauri/Cargo.lock
          git commit -m "chore(release): ${{ steps.ver.outputs.next }}

          bump: ${{ inputs.bump }} (${{ steps.ver.outputs.current }} → ${{ steps.ver.outputs.next }})
          由 Bump Version 工作流自动生成。"
          # 先推 commit 到 main，这会触发 CI 工作流
          git push origin "$GITHUB_REF_NAME"
          # 创建 tag 但暂不推送，等 CI 完成后再推
          git tag "$TAG"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "已推送 commit 到 main，CI 将被触发。等待 CI 完成后再推送 tag $TAG"

  # ------------------------------------------------------------
  # 等待 CI 工作流成功完成后再推送 tag
  # ------------------------------------------------------------
  wait-for-ci:
    name: Wait for CI
    needs: bump
    runs-on: ubuntu-latest
    steps:
      - name: Wait for CI workflow to complete
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          echo "等待 CI 工作流完成 (repo=$REPO)..."
          MAX_WAIT=1800  # 最长等待 30 分钟（三平台 release 首次构建约 15min，后续命中缓存约 5min）
          WAITED=0
          while [ "$WAITED" -lt "$MAX_WAIT" ]; do
            RUN=$(gh run list \
              --repo "$REPO" \
              --workflow=ci.yml \
              --branch=main \
              --limit=1 \
              --json status,conclusion,databaseId,headSha \
              --jq '.[0]')
            STATUS=$(echo "$RUN" | jq -r '.status')
            CONCLUSION=$(echo "$RUN" | jq -r '.conclusion')
            RUN_ID=$(echo "$RUN" | jq -r '.databaseId')
            SHA=$(echo "$RUN" | jq -r '.headSha')
            echo "CI run #$RUN_ID: status=$STATUS conclusion=$CONCLUSION sha=$SHA (已等待 ${WAITED}s)"
            if [ "$STATUS" = "completed" ]; then
              if [ "$CONCLUSION" = "success" ]; then
                echo "CI 成功完成！"
                exit 0
              else
                echo "::error::CI 工作流失败 (conclusion=$CONCLUSION)，取消发布"
                exit 1
              fi
            fi
            sleep 15
            WAITED=$((WAITED + 15))
          done
          echo "::error::等待 CI 超时（${MAX_WAIT}s）"
          exit 1

  # ------------------------------------------------------------
  # CI 成功后推送 tag，触发 Release
  # ------------------------------------------------------------
  push-tag:
    name: Push Tag (Trigger Release)
    needs: [bump, wait-for-ci]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}

      - name: Push tag
        run: |
          TAG="${{ needs.bump.outputs.tag }}"
          # tag 在 bump job 的 runner 本地创建但未推送，这里新 checkout 不会有该 tag
          # 直接在远程创建 tag（指向 main 最新 commit）
          SHA=$(git rev-parse HEAD)
          echo "在 commit $SHA 上创建并推送 tag: $TAG"
          # 用 GitHub API 创建 tag，确保跨 runner 可用
          gh api \
            --method POST \
            /repos/${{ github.repository }}/git/refs \
            -f ref="refs/tags/$TAG" \
            -f sha="$SHA" || {
              # 如果 tag 已存在（重试场景），直接 push
              echo "tag 可能已存在，尝试直接 push"
              git tag "$TAG" "$SHA" 2>/dev/null || true
              git push origin "$TAG" 2>/dev/null || true
            }
          echo "已推送 tag $TAG，Release 工作流将被触发"
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          echo "## 版本递增完成 ✅" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| 项目 | 值 |" >> "$GITHUB_STEP_SUMMARY"
          echo "|------|-----|" >> "$GITHUB_STEP_SUMMARY"
          echo "| 新版本 | ${{ needs.bump.outputs.next }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| 递增类型 | ${{ inputs.bump }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag | \`v${{ needs.bump.outputs.next }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "CI 已通过 ✅，tag 已推送，**Release** 工作流正在构建三平台产物。" >> "$GITHUB_STEP_SUMMARY"
