# RD 插件系统设计文档 v1.0

> 项目：**RD — 跨平台 SSH/SFTP 远程文件管理器**
> 仓库：https://github.com/zhangOranges/RD
> 技术栈：**Tauri 2.0 · React 19 · TypeScript 5.8 · Vite 7 · Zustand 5 · Rust 后端 · xterm.js 终端**
> 应用 ID：`rd-app`（`appDataDir` → Windows: `%APPDATA%\rd-app` / macOS: `~/Library/Application Support/rd-app` / Linux: `~/.config/rd-app`）
> 客户端当前版本：`0.1.89`（`package.json.version`）
> 文档版本：v1.0
> 更新日期：2026-08-15

---

## 目录

1. [概述](#一概述)
2. [插件系统整体架构](#二插件系统整体架构)
3. [插件标准规范](#三插件标准规范)
4. [插件 SDK API 契约](#四插件sdk-api-契约)
5. [事件总线规范](#五事件总线规范)
6. [权限系统规范](#六权限系统规范)
7. [插件管理器设计](#七插件管理器设计)
8. [插件 UI 界面设计](#八插件ui界面设计)
9. [安全沙箱设计](#九安全沙箱设计)
10. [热加载机制](#十热加载机制)
11. [端口转发插件专项规范](#十一端口转发插件专项规范)
12. [落地迭代路线图](#十二落地迭代路线图)
13. [附录：完整 SDK 类型定义](#十三附录完整sdk类型定义)

---

## 一、概述

### 1.1 项目现状（约束插件设计方向）

RD 已投产并具备以下基础能力（设计插件时**必须复用**，不允许重复造轮子）：

| 能力 | 代码位置 | 说明 |
|---|---|---|
| 主机配置 & 分类 | `src/types/index.ts` `HostConfig` / `CategoryConfig` + Rust `credentials.rs` | 凭据 base64 本地存储、支持密码/私钥认证、路径记忆 `path_cache_id` |
| 连接状态机 | `ConnectionState = disconnected \| connecting \| connected \| reconnecting` + `ReconnectMeta` | 4 态；断线后 5 层网络检测 + 指数退避自动重连（2→4→8→16→30s，最多 10 次） |
| 主机 & 连接管理 | `src/store/hostStore.ts` | 连接池、主动断开标记、cancelReconnect、连接成功后指纹返回 `ConnectResult` |
| SSH 终端 | `src/components/TerminalPanel.tsx` + `@xterm/xterm 6.0` | 多标签 PTY；重连后自动恢复 shell 会话；顶部横幅可取消重连/立即重试 |
| SFTP 双栏文件管理 | `src/components/FileBrowser.tsx` + `ContentArea.tsx` | 远程列表、本地栏、右键菜单、地址栏；重连期间全屏 overlay「尝试重连中…」；上传/下载支持临时文件→正式压缩包→解压策略 |
| 传输队列 | `src/store/transferStore.ts` + `TransferTask` | 排队/运行/完成/错误/取消；带 `speedBytesPerSec` 实时速率、进度事件 |
| 多主题 | `src/theme/palette.ts` + `themeStore` | 6 套主题（Dark / Light / Tech-dark / Eye-care / Solarized / Nord）；全部使用 CSS 变量，自动切换 |
| 设置对话框 7 个 Tab | `src/components/SettingsDialog.tsx` | 顺序：通用 → 终端 → 主题 → 自动更新 → 快捷键 → 调试 → 关于（**插件 Tab 插入「调试」与「关于」之间**） |
| 日志持久化 | `src/utils/log.ts` → Rust `update_log` 命令 | 写入 `appDataDir/updates/update.log`；info 仅调试模式写文件，warn/error 永久写 |
| 凭据打码 & 隐私 | `src/store/uiStore.ts` `maskMode` → `SettingsDialog` 开关 | 主机列表密码/密钥字段一键打码模糊，方便截图分享 |
| SSH 指纹验证 | Rust `connection.rs` | 首次接入提示指纹，防中间人攻击 |
| 自动更新 | `src/hooks/useAppUpdater.ts` → Tauri updater bundle target | 生成 `*.app.tar.gz / *.exe.zip / *.AppImage.tar.gz` + sig 签名 |
| CI / 构建目标 | `.github/workflows/ci.yml` + `release.yml` | Ubuntu CI（tsc/vite/rust check/clippy），Release 三平台：macOS ARM64 · Windows x64 NSIS · Linux x64 glibc（deb + AppImage） |

### 1.2 设计目标

- **低耦合**：插件不 import RD 业务模块（`store/* / components/*`），只依赖 `@rd/plugin-sdk` 单包
- **热插拔**：安装/启用/禁用/卸载**不重启 RD**（Tauri WebView + iframe 沙箱毫秒级切换）
- **可扩展**：覆盖连接 / 终端 / SFTP / 传输 / 告警 / 端口转发 / 云主机导入等运维场景
- **安全可控**：iframe 沙箱 + 权限白名单 + **凭据不外露约束** + 敏感操作二次确认
- **标准化**：数据模型 100% 对齐 `src/types/index.ts` 类型定义；不发明第二套类型
- **向后兼容**：SDK `apiVersion` 版本化；旧插件在新版 RD 保留旧分支运行

### 1.3 插件可扩展的能力范围

| 分类 | 可扩展场景 |
|------|-----------|
| **连接层** | 云服务器一键导入（阿里云 ECS / 腾讯云 CVM / 华为云 / AWS / Azure）、新协议扩展（RDP / Telnet / Serial over SSH）、MFA 二次验证流程 |
| **会话生命周期** | 连接前 hook（安全组白名单校验）、断线重连策略替换、会话标签自动打标、按规则自动断开 |
| **批量命令** | 自定义批量脚本、命令模板库、正则解析输出生成结构化报表、告警推送（钉钉 / 企业微信 / 飞书 / Slack / Webhook） |
| **文件传输** | 批量上传 / 下载、OSS/COS/S3 云存储互通、压缩解压、本地双向目录 sync、大文件分块断点续传 |
| **终端 UI** | 自定义终端主题 & 字体、快捷键自定义模板、终端水印、日志格式化（JSON 着色、Ansi 渲染增强）、敏感命令二次确认拦截 |
| **运维工具** | 服务器负载监控面板、磁盘空间巡检、Docker/K8s 快捷管理面板、数据库（MySQL/Redis/Mongo）快捷登录助手 |
| **系统增强** | 镜像源管理（一键换源）、配置跨设备云同步、手动备份 / 恢复、定时任务（Cron 编排界面） |
| **通知** | 批量执行完成、脚本异常告警、自定义邮件/IM 报告、主机下线实时通知 |
| **网络隧道** | SSH 端口转发管理（本地 -L / 远程 -R / SOCKS5 -D）、隧道健康检测、自动重连、导入导出 |

### 1.4 设计核心原则

1. **不读明文凭据**：插件 SDK `ServerApi` **永远不返回密码 / 私钥原文**，即使声明了高权限也不行
2. **复用连接池**：禁止插件新建独立 SSH 进程；`SshApi`/`SftpApi` 必须走 RD 的连接池
3. **传输任务统一**：SFTP 上传/下载必须走 `transferStore`，文件在目标目录先写临时文件名再 rename；遵守「临时压缩包 → 解压 → 删临时」的文件夹传输协议
4. **状态事件齐全**：reconnect 全生命周期必须暴露事件总线（`connection:reconnecting` 等），插件 UI 可跟随显示重连 overlay
5. **主题自适应**：插件注入的任何 UI 必须使用 CSS 变量（`var(--text-primary/--accent/--radius-lg)` 等），随 themeStore 自动切换
6. **日志统一桥接**：插件 `ctx.log` 复用 Rust `update_log` 命令，行前缀固定 `[plugin:{id}]`
7. **单一职责**：禁止"大杂烩"单插件，一个插件只解决一类场景
8. **UI 受控注入**：v1 插件不允许直接操作 DOM；React 组件形式仅允许注册到**固定容器槽位**

---

## 二、插件系统整体架构

### 2.1 三层架构（贴合 RD 实际分层）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       RD 主程序（内核层 - 不可 import）                   │
│                                                                          │
│   Rust 内核        credentials.rs 凭据  connection.rs SSH+SFTP+指纹      │
│                   update_log 日志写文件  transfer.rs 传输任务            │
│                   lib.rs Tauri Commands → 前端 invoke()                  │
│                                                                          │
│   前端 Store      hostStore / terminalStore / fileStore / transferStore  │
│                   uiStore (maskMode) / themeStore / shortcutStore        │
│                   historyStore / localFileStore                          │
│                                                                          │
│   UI 组件槽位     Toolbar  Sidebar  TabBar  FileBrowser (双栏)           │
│                   ContentArea  TerminalPanel  RightPanel (ServerInfo)    │
│                   SettingsDialog (7 Tab → 插入「插件」Tab→关于之前)      │
│                   StatusBar  TransferQueue                               │
└───────────────────────────────────┬─────────────────────────────────────┘
                                    │ 标准 SDK API + 事件总线 + 只读上下文
┌───────────────────────────────────▼─────────────────────────────────────┐
│                       插件管理器（调度层）                                │
│                                                                          │
│   PluginManager        扫描 / manifest 校验 / 生命周期 / 权限拦截        │
│   Sandbox（iframe）    隔离渲染 + postMessage 桥接 SDK API 调用          │
│   EventBus Bridge      内核事件 → 转发到各插件沙箱的 on() 监听           │
│   Plugin UI Host       把插件注册的按钮/菜单项插入对应组件槽位            │
│   Hot Reload Watcher   notify → 毫秒级重建 iframe（同源不刷新主窗）      │
└───────────────────────────────────┬─────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼─────────────────────────────────────┐
│                       第三方插件（业务扩展层）                            │
│                                                                          │
│   官方插件     端口转发 / 云主机导入 / 钉钉告警 / 监控巡检 / Docker 面板  │
│   第三方插件   自定义脚本模板 / 云存储互通 / 自动备份 / 任意扩展          │
│                                                                          │
│   执行形态     iframe 沙箱 + React 组件（可选）或纯逻辑（无 UI）          │
│   存储隔离     appDataDir/plugin-data/{pluginId}/* 独立子目录            │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 各层职责

#### 内核层（RD 主程序）

- **只读上下文 Context**：暴露 `HostConfig`（脱敏）、`ConnectionState`、`ReconnectMeta`、当前激活 Tab、主题 token、调试模式等
- **全局事件总线 EventBus**：连接 5 态（含 reconnect）+ 终端流 + SFTP 进度 + 主题切换 + 设置变更 + 自动更新 生命周期钩子
- **内置 SDK 工具包**：SSH 复用连接池、SFTP 复用传输队列、本地 `plugin-data/` 文件、受控 HTTP 请求、Toast / Dialog / Confirm / Prompt、Tab 切换
- **权限控制中心**：调用 SDK 前强制校验权限声明；凭据字段强制脱敏（即使声明 server.read 也不返回密文）

#### 调度层（插件管理器）

- 插件源目录扫描：`${appDataDir}/plugins/*.rdplugin`（zip 包解压目录）
- manifest 解析 → `apiVersion` / `minRdVersion` 兼容校验 → 依赖 / 冲突判定
- `iframe` 沙箱创建（`sandbox="allow-scripts allow-same-origin"` 按需）；通过 `postMessage` 传输 SDK 调用
- 生命周期调度（安装 → 解析 → init → enable → 运行 → disable → 卸载）
- EventBus 桥接：内核 `emit` → 找到订阅该事件的插件 → postMessage 到沙箱
- 权限校验：每个 SDK 调用到 Rust / HostStore 之前先 `sandbox.assertPerm(pluginId, perm)`
- 插件管理 UI（设置页「插件」Tab）

#### 扩展层（第三方插件）

- 每个插件独立 `.rdplugin` 包（zip）或未打包开发目录；结构标准化
- 完全隔离：插件脚本运行在 iframe context，无法访问 `window.__TAURI_INTERNALS__`
- 仅通过 `@rd/plugin-sdk` 暴露的 API 与主程序交互
- UI 注入点：**固定槽位**（Toolbar/Sidebar/TabBar/FileBrowser 右键/Settings 子 Tab/RightPanel 侧边抽屉）

---

## 三、插件标准规范

### 3.1 插件目录结构

```
rd-plugin-xxx/                 # 开发时目录；打包生成 rd-plugin-xxx.rdplugin (zip)
├─ manifest.json               # 【必填】插件元数据，加载入口
├─ index.html                  # 【必填】iframe 沙箱入口 HTML（可选渲染 React 根）
├─ main.ts                     # 插件入口（继承 BasePlugin，打包后 main.js）
├─ config.schema.json          # 插件配置表单结构（自动生成配置面板 + 持久化）
├─ assets/                     # 静态资源
│  └─ icon.png                 # 256×256 透明圆角图标（推荐沿用 RD 母图风格）
├─ src/                        # 可选：TypeScript / React 子组件 / 业务模块
│  ├─ App.tsx
│  └─ utils.ts
├─ LICENSE
└─ README.md                   # 插件使用说明、兼容 RD 版本、权限声明说明
```

### 3.2 `manifest.json` 规范

插件身份定义文件；**唯一可信来源**，加载时强校验 schema。

```json
{
  "id": "cloud-ecs-import",
  "name": "云服务器批量导入",
  "version": "1.0.0",
  "apiVersion": "v1",
  "author": "zhangOranges",
  "description": "一键导入阿里云 / 腾讯云 / 华为云 ECS 主机到 RD 分类列表",
  "category": "connection",
  "entry": "./main.js",
  "indexHtml": "./index.html",
  "configSchema": "./config.schema.json",
  "permissions": [
    "network.http",
    "storage.read",
    "storage.write",
    "server.write"
  ],
  "conflict": ["rd-plugin-other-ecs-importer"],
  "requires": [],
  "icon": "./assets/icon.png",
  "minRdVersion": "0.1.89",
  "hotReload": true
}
```

#### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | ✅ | 全局唯一；`kebab-case`；不得与 RD 内部保留前缀（`rd-native:*`）冲突 |
| `name` | string | ✅ | 显示名称（中文允许） |
| `version` | string | ✅ | semver 语义化版本 |
| `apiVersion` | `'v1'` | ✅ | SDK API 版本；v2 后会保留兼容分支 |
| `author` | string | ✅ | 作者 GitHub ID 或名字 |
| `description` | string | ✅ | 不超过 120 字的简介 |
| `category` | enum | ✅ | `connection` / `command` / `file` / `terminal` / `monitor` / `notify` / `tunnel` / `other` |
| `entry` | string | ✅ | 插件入口 JS（打包产物相对路径） |
| `indexHtml` | string | ✅ | iframe 沙箱 HTML 入口（有 UI 组件的插件必填；纯逻辑插件可填和 entry 同目录） |
| `configSchema` | string | ❌ | 配置表单 schema 路径；缺省则插件无独立配置面板 |
| `permissions` | string[] | ✅ | 权限白名单（见第六章）；**高危权限会在安装时红色高亮并二次确认** |
| `conflict` | string[] | ❌ | 互斥插件 ID（例如两个"端口转发"插件不能同时启用） |
| `requires` | string[] | ❌ | 依赖其他插件；启用前会校验依赖链 |
| `icon` | string | ❌ | 图标路径；未填则用默认 RD 拼图图标 + 插件首字 |
| `minRdVersion` | string | ✅ | 最低 RD 客户端版本；当前已发布 `0.1.89` |
| `hotReload` | boolean | ✅ | 是否支持热加载；签名插件建议为 `false` 以防篡改运行 |

### 3.3 插件生命周期（完全对齐 BasePlugin）

| 生命周期 | 触发时机 | 职责 |
|---------|---------|------|
| `init(ctx, bus)` | 插件 manifest 解析 + 沙箱创建完成 | 保存 ctx/bus；注册事件监听；**不得注册任何 UI** |
| `enable()` | 用户点「启用」或应用启动后该插件已启用 | 注册 Toolbar 按钮、Sidebar 菜单、Settings 子 Tab、右键菜单等 UI 入口 |
| `disable()` | 用户禁用 / 热加载重载前 / RD 关闭 | 注销全部 UI 注册、事件监听、`setInterval`、`setTimeout`；**必须释放** |
| `uninstall()` | 用户卸载 | 清空 `${appDataDir}/plugin-data/{id}/` 全部存储、配置、临时文件；由内核兜底清空 |
| `onConfigChange(newConfig)` | 用户保存配置面板或 `plugin_set_config` 被调用 | 应用新配置，无 UI 阻塞 |

#### 标准入口示例（对齐 `types/index.ts` 字段）

```typescript
import {
  BasePlugin,
  RDContext,
  EventBus,
  HostConfig,
  CategoryConfig,
  ConnectionState,
} from '@rd/plugin-sdk'

export default class EcsImportPlugin extends BasePlugin {
  private ctx!: RDContext
  private bus!: EventBus

  async init(ctx: RDContext, bus: EventBus) {
    this.ctx = ctx
    this.bus = bus
    this.bus.on('connection:success', (hostId, state) => {
      this.ctx.log.info(`主机 ${hostId} 已连接：state=${state}`)
    })
  }

  async enable() {
    this.ctx.ui.registerSidebarAction({
      id: 'ecs-import-btn',
      label: '导入云主机',
      icon: 'Cloud',
      onClick: () => this.openImportWizard(),
    })
    this.ctx.ui.registerSettingsSubTab({
      id: 'ecs-import-config',
      label: '云主机导入',
      group: 'plugins',
      render: 'schema',
    })
  }

  async disable() {
    this.ctx.ui.removeSidebarAction('ecs-import-btn')
    this.ctx.ui.removeSettingsSubTab('ecs-import-config')
    this.bus.offAll(this)
  }

  async uninstall() {
    await this.ctx.storage.removeAll()
  }

  async onConfigChange(cfg: Record<string, unknown>) {
    this.ctx.log.info(`AK/SK 已更新：${cfg.aliyunAk ? '已配置' : '未配置'}`)
  }

  private async openImportWizard() {
    const categories: CategoryConfig[] = await this.ctx.server.listCategories()
    this.ctx.log.info(`当前分类：${categories.map(c => c.name).join(', ')}`)
    // TODO: 调阿里云 API → this.ctx.server.add(hostConfig) 批量导入
  }
}
```

### 3.4 配置表单规范（`config.schema.json`）

与 RD SettingsDialog 内置组件风格 100% 一致；自动读/写持久化配置，保存后**自动触发** `onConfigChange(config)`。

**支持字段类型（JSON Schema 子集）**

| schema | 渲染组件（对齐 RD 原生） |
|--------|-------------------------|
| `type: string` | 单行输入框（`.settings-input`） |
| `type: string` + `format: password` | 密码输入框（打码；**插件 SDK 中拿到的值仍为明文（插件自需保护），但不会写系统日志**） |
| `type: string` + `format: textarea` | 多行文本域（`.settings-textarea`） |
| `type: number` | 数字步进输入；可选 `minimum / maximum` |
| `type: boolean` | `.switch` 开关（同"调试日志"开关视觉） |
| `type: string` + `enum: [...]` | 下拉选择框 |
| `type: object` + `properties` | 分组框（折叠分组面板） |

#### 示例（云主机导入插件）

```json
{
  "type": "object",
  "title": "云主机导入配置",
  "properties": {
    "aliyunAk": {
      "type": "string",
      "title": "阿里云 AccessKey ID",
      "format": "password"
    },
    "aliyunSk": {
      "type": "string",
      "title": "阿里云 AccessKey Secret",
      "format": "password"
    },
    "defaultCategoryId": {
      "type": "string",
      "title": "导入至默认分类",
      "description": "留空则导入到默认「未分类」分组"
    },
    "syncIntervalMin": {
      "type": "number",
      "title": "自动同步间隔（分钟）",
      "minimum": 0,
      "maximum": 1440,
      "default": 0,
      "description": "填 0 表示关闭自动同步"
    },
    "tagAutoImport": {
      "type": "boolean",
      "title": "按实例标签自动为 RD 主机打 tags",
      "default": true
    }
  }
}
```

### 3.5 插件打包格式

- **文件后缀**：`.rdplugin`
- **本质**：标准 zip 压缩包；根目录就是上面的标准插件目录
- **MIME / 扩展名**：RD 关联 `.rdplugin`，双击即启动安装流程
- **安装路径**：解压至 `${appDataDir}/plugins/${pluginId}@${version}/`
- **数据存储**：运行时数据写入 `${appDataDir}/plugin-data/${pluginId}/`（**卸载时清空**；配置文件存在 `config.json`）

---

## 四、插件 SDK API 契约

> 插件开发者**只允许依赖 `@rd/plugin-sdk` 单包**；绝不允许 `import 'rd/src/store/hostStore'` 等内部模块，沙箱层会阻断。

### 4.1 RDContext（插件上下文）

```typescript
interface RDContext {
  readonly pluginId: string
  readonly manifest: PluginManifest

  // SDK 分组
  ui: UiApi
  storage: PluginStorage
  ssh: SshApi
  sftp: SftpApi
  server: ServerApi
  http: HttpApi
  tunnel: TunnelApi
  theme: ThemeApi

  // 日志桥 → 复用 Rust update_log
  log: {
    info(msg: string): void   // 仅调试模式写文件
    warn(msg: string): void   // 永久写文件
    error(msg: string): void  // 永久写文件 + 可能触发"插件异常"告警
  }
}
```

> 行格式统一：`[plugin:${pluginId}] ${msg}`。用户可在「调试」Tab 的设置中勾选「隐藏指定插件日志」。

### 4.2 UI API（贴合 RD 组件槽位）

```typescript
interface ToolbarButtonOption {
  id: string
  label: string
  icon?: string              // lucide-react icon name；未填显示首字
  tooltip?: string           // Tooltip 悬浮文案
  group?: 'left' | 'center' | 'right'  // 默认 right
  onClick: () => void | Promise<void>
}

interface SidebarActionOption {
  id: string
  label: string
  icon?: string
  location: 'category-header' | 'host-item-menu' | 'sidebar-footer'
  onClick: (ctx: { categoryId?: string; hostId?: string }) => void
}

interface FileContextMenuOption {
  id: string
  label: string
  icon?: string
  scope: 'local' | 'remote' | 'both'  // 显示在哪个侧栏
  kinds: ('file' | 'folder' | 'empty')[]  // 作用目标类型
  onClick: (ctx: {
    hostId?: string
    side: 'local' | 'remote'
    path: string
    selectedPaths: string[]
  }) => void
}

interface SettingsSubTabOption {
  id: string
  label: string
  icon?: string
  group: 'general' | 'terminal' | 'theme' | 'update' | 'shortcut' | 'debug' | 'plugins' | 'about'
  render: 'schema'   // v1 仅支持按 schema 渲染；v2 引入自定义组件
}

interface RightPanelOption {
  id: string
  label: string
  icon?: string
  render: 'schema'
}

interface UiApi {
  // ---------- Toolbar 顶部工具栏 ----------
  registerToolbarButton(opt: ToolbarButtonOption): void
  removeToolbarButton(id: string): void

  // ---------- Sidebar 左侧主机树 ----------
  registerSidebarAction(opt: SidebarActionOption): void
  removeSidebarAction(id: string): void

  // ---------- FileBrowser 文件右键菜单 ----------
  registerFileContextMenu(opt: FileContextMenuOption): void
  removeFileContextMenu(id: string): void

  // ---------- Settings 对话框自定义子 Tab ----------
  registerSettingsSubTab(opt: SettingsSubTabOption): void
  removeSettingsSubTab(id: string): void

  // ---------- RightPanel 右侧抽屉面板 ----------
  openRightPanel(opt: RightPanelOption): void
  closeRightPanel(id: string): void

  // ---------- 系统级对话框 & Toast ----------
  notify(
    title: string,
    message: string,
    kind?: 'info' | 'success' | 'error' | 'warning',
    durationMs?: number,
  ): void

  confirm(message: string, title?: string): Promise<boolean>
  prompt(message: string, defaultValue?: string, title?: string): Promise<string | null>

  // ---------- 跳转与打开 ----------
  openPluginConfig(pluginId?: string): void           // 默认打开自己的
  focusHost(hostId: string): void                      // 切到指定主机 Tab
  focusTerminal(terminalId: string): void              // 切到指定终端
}
```

### 4.3 存储 API（插件隔离存储）

> 物理路径：`${appDataDir}/plugin-data/${pluginId}/store.db`（实际用 Tauri `@tauri-apps/plugin-fs` 写 JSON 即可）；key 自动前缀 `plugin:${id}:`。

```typescript
interface PluginStorage {
  set(key: string, value: unknown): Promise<void>
  get<T = unknown>(key: string): Promise<T | null>
  remove(key: string): Promise<void>
  removeAll(): Promise<void>

  // 读写自身插件下的二进制/文本大文件
  writeFile(name: string, content: Uint8Array | string): Promise<void>
  readFile(name: string): Promise<Uint8Array | null>
  listFiles(): Promise<string[]>
}
```

### 4.4 SSH API（复用连接池，绝对禁止新建独立 SSH 进程）

> 对应 hostStore 的连接池；失败时"人话"错误文案由内核 `classifyConnectFailure()` 统一返回。

```typescript
interface CommandResult {
  success: boolean
  output: string
  exitCode: number
}

interface SshApi {
  runCommand(
    hostId: string,
    cmd: string,
    opt?: { timeoutMs?: number; cwd?: string },
  ): Promise<CommandResult>

  /** 终端流：只读订阅 */
  onTerminalOutput(
    terminalId: string,
    listener: (data: Uint8Array) => void,
  ): () => void
}
```

### 4.5 SFTP API（复用 transferStore 传输队列；遵守临时文件协议）

> 文件夹上传/下载必须遵守：目标目录先出现 **临时压缩包**（带 `.tmp<pid>` 后缀）→ rename 为正式 zip → 解压 → 删除临时文件（详见项目约束 `project_memory.md`）。SDK 内部已封装，插件直接调用即可。

```typescript
interface SftpFile {
  name: string
  path: string
  isDir: boolean
  size: number
  modifiedAt: number
  permissions?: string
}

interface TransferHandle {
  taskId: string
  abort(): Promise<void>
  onProgress(listener: (bytes: number, total: number, speedBytesPerSec: number) => void): void
  finished(): Promise<'completed' | 'error' | 'canceled'>
}

interface SftpApi {
  list(hostId: string, remotePath: string): Promise<SftpFile[]>
  stat(hostId: string, remotePath: string): Promise<SftpFile | null>
  mkdir(hostId: string, remotePath: string): Promise<void>
  remove(hostId: string, remotePath: string, recursive?: boolean): Promise<void>
  rename(hostId: string, from: string, to: string): Promise<void>
  readText(hostId: string, remotePath: string): Promise<string>
  writeText(hostId: string, remotePath: string, content: string): Promise<void>

  // 大文件上传下载（接入 transferStore；显示在 TransferQueue 列表）
  upload(hostId: string, localPath: string, remotePath: string): TransferHandle
  download(hostId: string, remotePath: string, localPath: string): TransferHandle
}
```

### 4.6 服务器管理 API（100% 脱敏返回）

> 返回的 `HostConfig` / `CategoryConfig` 类型与 `src/types/index.ts` 一致。**关键安全约束**：`server.listAll()` 绝不为任何插件返回 password / privateKey 原文，即使权限最高也只能读到 `auth_type` + `has_password: boolean`（布尔存在标记）。

```typescript
// 插件可见的 HostConfig（凭据脱敏）
interface HostConfigSafe {
  id: string
  name: string
  host: string
  port: number
  username: string
  auth_type: 'password' | 'key'
  remember_dir: boolean
  remark: string
  category_id: string
  path_cache_id?: string
  // —— 额外辅助字段 ——
  has_password: boolean        // 是否已经保存过密码（仅布尔，不返回值）
  has_private_key: boolean     // 是否已经保存私钥
}

interface ServerApi {
  // 主机
  listAll(): Promise<HostConfigSafe[]>
  get(hostId: string): Promise<HostConfigSafe | null>
  add(item: Omit<HostConfig, 'id'> & {
    password?: string | null
    private_key?: string | null
  }): Promise<HostConfig>
  update(hostId: string, patch: Partial<Omit<HostConfig, 'id'>> & {
    password?: string | null
    private_key?: string | null
  }): Promise<HostConfig>
  remove(hostId: string): Promise<void>

  // 分类
  listCategories(): Promise<CategoryConfig[]>
  addCategory(name: string, order?: number): Promise<CategoryConfig>
  updateCategory(id: string, patch: Partial<CategoryConfig>): Promise<CategoryConfig>
  removeCategory(id: string): Promise<void>

  // 连接状态（实时）
  getConnectionState(hostId: string): Promise<{
    state: ConnectionState
    reconnectMeta?: ReconnectMeta
    homeDir?: string
    fingerprint?: string
  }>
  connect(hostId: string): Promise<void>            // 触发连接（非插件主动断开，不影响重连策略）
  disconnect(hostId: string, options?: {
    suppressReconnect?: boolean   // 默认 true；false 则断开后进入重连（不推荐插件调 false）
  }): Promise<void>
  cancelReconnect(hostId: string): Promise<void>
}
```

### 4.7 HTTP API（受控网络请求）

> 白名单制 + 域名权限；插件声明 `network.http` 后仍然**无法访问内网 IP（`10/8 / 172.16/12 / 192.168/16 / 127/8`）**，除非用户在插件配置里额外开启。

```typescript
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH'

interface HttpApi {
  request<T = unknown>(
    url: string,
    opt?: {
      method?: HttpMethod
      headers?: Record<string, string>
      body?: unknown   // 对象→JSON 序列化；二进制写 Uint8Array
      timeoutMs?: number
    },
  ): Promise<T>

  get<T = unknown>(url: string, headers?: Record<string, string>): Promise<T>
  post<T = unknown>(url: string, body?: unknown, headers?: Record<string, string>): Promise<T>
}
```

### 4.8 Theme API（多主题自动跟随）

```typescript
interface ThemeApi {
  getCurrent(): Promise<{ id: string; name: string; cssVars: Record<string, string> }>
  onChange(listener: (themeId: string) => void): () => void

  // 给插件 iframe 注入主题变量；沙箱内 CSS 写 var(--text-primary) 就能跟随
  syncToIframe(win: Window): void
}
```

### 4.9 隧道 API（端口转发）

详见第十一章。

---

## 五、事件总线规范

### 5.1 接口

```typescript
interface EventBus {
  on<T extends keyof RdEventMap>(
    event: T,
    handler: (...args: RdEventMap[T]) => void,
    owner?: object,
  ): void
  off<T extends keyof RdEventMap>(event: T, handler: (...args: RdEventMap[T]) => void): void
  offAll(owner: object): void
  emit<T extends keyof RdEventMap>(event: T, ...args: RdEventMap[T]): void
}
```

> `owner` 用于 `disable()` 时一键解绑（`bus.offAll(this)`）；插件必须在 `disable()` 中调用，否则内存泄漏。

### 5.2 完整事件列表（按 RD 真实流程）

#### A. 连接 & 自动重连（重点：新增 reconnect 全生命周期）

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `connection:before-connect` | `host: HostConfig` | 发起连接前（可用于日志 hook；v1 不可拦截修改） |
| `connection:success` | `hostId: string, result: ConnectResult` | SSH 握手完成、home_dir 与 fingerprint 返回 |
| `connection:error` | `hostId: string, friendly: FriendlyFailure, rawError: string` | 连接失败；friendly 是内核已经分类的"人话"文案 |
| `connection:close` | `hostId: string, reason: 'user' \| 'passive'` | 会话断开；`user`=主动断开，不会触发重连 |
| **`connection:reconnecting`** | `hostId: string, meta: ReconnectMeta` | 进入自动重连流程（UI 侧变黄闪 + overlay） |
| **`connection:reconnect-attempt`** | `hostId: string, attempt: number, nextDelayMs: number` | 单次重连发起 |
| **`connection:reconnect-success`** | `hostId: string, result: ConnectResult` | 某次重连成功 → 回到 connected |
| **`connection:reconnect-failed`** | `hostId: string, attempt: number, nextAt: number` | 某次重连失败 → 继续退避等待 |
| **`connection:reconnect-aborted`** | `hostId: string, reason: 'canceled' \| 'timeout' \| 'user-disconnected'` | 重连流程停止（取消或 5 分钟超时） |
| `connection:batch-start` | `batchId: string, hostIds: string[]` | 批量连接/执行开始 |
| `connection:batch-finish` | `batchId: string, results: BatchResult` | 批量完成 |

#### B. 终端

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `terminal:new-tab` | `terminalId: string, hostId?: string` | 用户新建终端 Tab |
| `terminal:output` | `terminalId: string, data: Uint8Array` | xterm 输出流 |
| `terminal:input` | `terminalId: string, input: string` | 用户输入回车确认 |
| `terminal:resize` | `terminalId: string, cols: number, rows: number` | 终端尺寸变化 |
| `terminal:closed` | `terminalId: string` | 终端标签关闭 |
| `terminal:theme-change` | `themeId: string, palette: TerminalPalette` | 终端主题变化（可用于日志配色插件） |

#### C. SFTP 文件 & 传输

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `sftp:list-finish` | `hostId: string, path: string, files: SftpFile[]` | 远程目录刷新完成 |
| `sftp:navigate` | `hostId: string, from: string, to: string` | 用户在 SFTP 面板跳转目录 |
| `sftp:upload-start` | `task: TransferTask` | 上传任务加入队列并开始 |
| `sftp:upload-progress` | `taskId: string, bytesTransferred: number, totalBytes: number, speedBps: number` | 上传进度（可用于插件统计/图表） |
| `sftp:upload-finish` | `taskId: string, status: TransferStatus, errorMsg?: string` | 上传完成/错误/取消 |
| `sftp:download-start` / `sftp:download-progress` / `sftp:download-finish` | 同上 | 下载对应事件 |
| `sftp:file-delete` | `hostId: string, side: 'local' \| 'remote', path: string, isDir: boolean` | 删除文件或目录 |
| `sftp:file-rename` | `hostId: string, side: 'local' \| 'remote', from: string, to: string` | 重命名 |

#### D. 主机 & 分类 CRUD

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `host:added` | `host: HostConfigSafe` | 新增主机（包括插件 `server.add()` / 用户手工 / 云导入） |
| `host:updated` | `hostId: string, patch: Partial<HostConfigSafe>` | 编辑主机 |
| `host:removed` | `hostId: string` | 删除主机 |
| `category:added` / `category:updated` / `category:removed` | `category: CategoryConfig` | 分类增删改 |

#### E. UI & 主题 & 设置

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `ui:theme-change` | `themeId: string` | themeStore 切换主题；插件自定义组件的 CSS var 自动跟随 |
| `ui:mask-mode-change` | `masked: boolean` | 一键打码开关变化；插件如渲染密码字段应跟随 |
| `ui:settings-change` | `changed: { general?: ..., terminal?: ..., debug?: ... }` | 设置对话框保存后 |
| `ui:tab-change` | `tabId: string, tabType: 'sftp' \| 'terminal'` | TabBar 切换 |
| `ui:notification` | `kind: ToastKind, message: string` | 内核即将弹 Toast；可用于插件记录审计 |
| `ui:window-blur` / `ui:window-focus` | `windowId: string` | Tauri 窗口焦点变化（敏感命令插件可自动锁屏） |

#### F. 隧道（端口转发）

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `tunnel:start` | `tunnelId: string, status: TunnelStatus` | 隧道成功启动监听 |
| `tunnel:stop` | `tunnelId: string` | 隧道已关闭（SSH 会话断开 → 内核先关再 emit） |
| `tunnel:error` | `tunnelId: string, code: TunnelErrorCode, msg: string` | 隧道启动失败 / 运行中断 |
| `tunnel:connection` | `tunnelId: string, peer: { addr: string; port: number }` | 有客户端连入本地监听端口（审计/监控插件用） |

#### G. 自动更新 & 系统日志

| 事件 | 参数 | 触发时机 |
|---|---|---|
| `updater:check-finished` | `found: boolean, version?: string` | 启动时检查更新完成 |
| `updater:downloading` | `bytes: number, total: number` | 更新包下载中 |
| `updater:ready-to-install` | `version: string, sigVerified: boolean` | 下载完成 + 签名校验通过 |
| `log:write` | `level: 'info' \| 'warn' \| 'error', tag: string, msg: string` | 日志写入；插件不要订阅全部，否则巨量 |
| `config:sync` | `timestamp: number` | 设置 & 主机列表 JSON 备份写出时（可用于云同步插件触发上传） |

---

## 六、权限系统规范

### 6.1 权限列表（对齐 RD 代码行为）

| 权限标识符 | 说明 | 风险等级 | 约束 |
|---|---|---|---|
| `network.http` | 受控 HTTP 请求 | 中 | 默认阻止内网；用户可在插件详情打开"允许内网 API" |
| `storage.read` | 插件隔离存储读取 | 低 | 仅读自身 `plugin-data/${id}/` |
| `storage.write` | 插件隔离存储写入 | 低 | 仅写自身目录 |
| `file.local.read` | 读取本地其他目录 | 中 | 需二次选文件对话框；禁止读 `~/.ssh/`、RD 凭据目录 |
| `file.local.write` | 写入本地其他目录 | 中 | 同上 |
| `server.read` | 读主机 / 分类列表（**脱敏**） | 中 | 永远不返回密码/私钥原文 |
| `server.write` | 增 / 改 / 删主机 | 高 | 安装时二次确认；UI 红色标红 |
| `server.manage` | connect / disconnect / cancelReconnect | 高 | 含 `disconnect(suppressReconnect=false)` 的重连影响操作 |
| `ssh.run` | 执行 SSH 命令 | **极高** | 写入审计日志；支持 RD 全局"禁止所有插件执行命令"总开关 |
| `sftp.operate` | SFTP 列目录 / 上传 / 下载 / 删除 | 高 | 文件夹传输必须走临时文件→rename 协议；审计日志 |
| `ui.notification` | Toast 通知 | 低 | 限频（10s / 插件最多 3 条） |
| `ui.dialog` | Confirm / Prompt 对话框 | 低 | 限频（5s / 插件最多 2 条） |
| `ui.inject-menu` | 注册 Toolbar / Sidebar / 右键菜单项 | 低 | 内置启用/禁用即可 |
| `theme.read` | 读主题 token | 低 | 只读 |
| `tunnel.manage` | 管理 SSH 端口转发（含远程 -R） | **极高** | 支持全局"禁止远程转发 (-R)"；0.0.0.0 监听需二次确认 |

### 6.2 权限机制

1. **声明制**：manifest `permissions` 声明
2. **授权制**：安装弹窗展示**权限 + 风险等级 + 后果说明**，用户点「确认授予」才写入 `granted_permissions`
3. **可撤回**：插件详情 Tab「权限管理」面板单条撤销
4. **拦截制**：无声明 → 调用直接抛 `PERMISSION_DENIED` 并写 warn 日志
5. **高危提示**：`ssh.run / server.write / tunnel.manage / sftp.operate` 这 4 项安装时**红色大卡片 + 复选框用户手点「我理解风险」**才可确认
6. **凭据保护为内核约束**：即使权限全开也无法读取密码/私钥字段 —— 这是 SDK 脱敏层的硬编码，不被权限配置影响

### 6.3 全局开关（设置 → 插件 Tab）

对齐 SettingsDialog 现有「调试 / 关于」附近的风格，提供以下 3 个大 `.switch` 开关：

- 🔒 **禁止所有插件执行 SSH 命令**（对应 `ssh.run` 全局拦截）
- 🔒 **禁止所有插件修改主机配置**（`server.write` 拦截）
- 🔒 **禁止远程端口转发（-R 模式）**（`tunnel.manage` 的 `-R` 子模式拦截）
- ⚙️ **允许插件访问内网 HTTP API**（默认关；影响所有 `network.http`）
- ⚙️ **隐藏插件日志**（一键在调试 Tab 日志视图里过滤掉所有插件行）

---

## 七、插件管理器设计

### 7.1 前端模块拆分

放在 `src/store/pluginStore.ts` + `src/components/settings/PluginsTab.tsx`（新文件）：

```
src/
├─ store/
│  └─ pluginStore.ts          # zustand store：插件列表/状态/权限/热重载调度
├─ components/
│  ├─ settings/
│  │  └─ PluginsTab.tsx       # SettingsDialog 插件 Tab 3 个子 Tab 面板
│  └─ plugin/
│     ├─ PluginSandbox.tsx    # iframe 封装 + postMessage 桥接（核心）
│     ├─ PluginUiHost.tsx     # 把插件注册的 UI 项插入到全局槽位
│     └─ HotReloadWatcher.ts  # 开发模式下监视插件目录变化（chokidar）
├─ types/
│  └─ plugin.ts               # PluginManifest / PluginState / SDK 类型
└─ utils/
   └─ pluginSdk.ts            # 内核侧 SDK API 实现 + 权限校验桥接
```

### 7.2 Rust 侧模块（`src-tauri/src/plugin/mod.rs`）

```
src-tauri/src/plugin/
├─ mod.rs           # pub mod *；对外暴露 init(app_handle)
├─ manifest.rs      # serde_json 解析 + jsonschema 校验（P5 v1 会用）
├─ manager.rs       # PluginManager：扫描/安装/卸载/启用禁用/持久化
├─ permissions.rs   # 权限校验：check(pluginId: &str, perm: &str) -> Result<()>
├─ store.rs         # SQLite / JSON 文件持久化（启用状态、granted_permissions、config）
├─ bridge.rs        # 接收前端 PluginSandbox 的 postMessage → Tauri 命令 → 返回结果
└─ hot_reload.rs    # notify crate 文件监听；debounce 500ms → 调用前端重新创建 iframe
```

### 7.3 Tauri Commands（前端 invoke → Rust）

```rust
// ===== 基础：列表 & 启用/禁用 =====
fn plugin_list() -> Vec<PluginInfo>
fn plugin_toggle(id: String, enabled: bool) -> Result<(), PluginError>
fn plugin_uninstall(id: String) -> Result<(), PluginError>

// ===== 本地安装 =====
fn plugin_install_from_file(zip_path: String) -> Result<PluginInfo, PluginInstallError>
fn plugin_install_from_dir(dir_path: String) -> Result<PluginInfo, PluginInstallError>

// ===== 配置（config.schema.json 自动表单） =====
fn plugin_get_config(id: String) -> Result<serde_json::Value>
fn plugin_set_config(id: String, config: serde_json::Value) -> Result<serde_json::Value>

// ===== 权限 =====
fn plugin_get_granted(id: String) -> Result<Vec<String>>
fn plugin_set_granted(id: String, perms: Vec<String>) -> Result<Vec<String>>

// ===== 热重载 =====
fn plugin_reload(id: String) -> Result<PluginInfo>
fn plugin_dev_watch(id: String, watch: bool) -> Result<bool>
```

### 7.4 插件持久化存储

```rust
#[derive(Serialize, Deserialize, Clone)]
struct PluginStoreItem {
    pub id: String,
    pub version: String,
    pub enabled: bool,
    pub install_time_ms: i64,
    pub last_load_time_ms: i64,
    pub granted_permissions: Vec<String>,
    pub config: serde_json::Value,
    pub load_error: Option<String>, // 加载失败时保存，UI 直接显示
}
```

物理存放：`${appDataDir}/plugin-state.json`（或 `plugin-state.db` 单表 sqlite）。

---

## 八、插件 UI 界面设计

### 8.1 设计原则

1. **不破坏 RD 现有布局**：所有注入点通过 UiApi 注册；`disable()` 时全部自动移除
2. **主题 100% 跟随**：必须使用 CSS 变量 `--text-primary / --text-secondary / --accent / --bg-content / --radius-md` 等（见 `theme/palette.ts`），禁止写死颜色
3. **SDK 受控**：插件不能 `document.getElementById` / `querySelector` / 操作 DOM；UI 一律走声明式注册
4. **隐私模式配合**：当 `uiStore.maskMode = true` 时，插件若展示主机敏感字段需自动打码（SDK 会提供 `ui.mask()` 工具函数）
5. **异常友好**：React 组件沙箱内部错误 → 显示「插件 xxx UI 出错」红框，**绝不崩溃整个 RD**

### 8.2 设置页「插件」Tab（在「调试」与「关于」之间插入，作为第 7 个 Tab）

SettingsDialog 当前顺序：

```
通用 → 终端 → 主题 → 自动更新 → 快捷键 → 调试 → 【新：插件】 → 关于
```

「插件」Tab 内又分 3 个子 Tab（顶部横向切，类似自动更新 Tab 的结构）：

#### 子 Tab 1：已安装插件

布局：**左侧滚动列表 + 右侧详情面板**

- **左侧列表行**
  - 首列：`assets/icon.png`（56×56，**圆角 14px** + 投影）+ 名称 + 分类小徽章（connection/tunnel/notify...）
  - 次列：版本号 + 作者名（次级文案）
  - 尾列：**状态标签**（绿「已启用」 / 灰「已禁用」 / 红「加载失败」带 tooltip 错误栈）+ 开关（`.switch`）+ 三个图标按钮（⚙ 配置 / 🛡 权限 / 🗑 卸载）

- **右侧详情面板**
  - 头部：大图标（96×96）+ 名称 + 版本胶囊 + 作者 + 简介
  - 元信息卡片：插件 ID / apiVersion / `minRdVersion` / 分类 / 安装时间 / 是否启用热加载
  - **权限卡**：每行权限名称 + 说明 + 风险等级小圆点（绿/黄/红）+ 单条撤销复选；高危权限（4 个）分组红色边框
  - 配置卡：若有 `config.schema.json` 直接在右侧详情面板内嵌入渲染（省得多跳窗）
  - 底部操作：【打开插件配置弹窗】 【管理权限】 【取消加载】 【卸载】

#### 子 Tab 2：本地安装

- **拖拽区**：大虚线圆角框（`400x240`，居中对齐 Finder 风格），文案「拖入 `.rdplugin` 文件开始安装 / 或点击选择文件」
- **选择文件按钮**：调用 Tauri `@tauri-apps/plugin-dialog` 的 `open`，filter `.rdplugin`
- **安装确认弹窗（核心安全）**（独立对话框，宽度 560px，圆角 `--radius-lg`）：
  1. 插件图标 + 名称 + 版本 + 作者（头部）
  2. 风险提示条（背景 `--bg-danger-soft`）：「该插件为第三方代码，RD 未对其进行官方审计，请在确认信任后安装」
  3. **权限清单卡片**：分组展示；高危权限展开详细说明 + 影响后果
  4. 「我已阅读并理解以上风险」复选框（不勾选按钮禁用）
  5. 【取消】｜【确认授予权限并安装】

#### 子 Tab 3：开发者控制台

- **顶部工具条**：
  - 下拉选插件（默认全部）
  - 级别过滤（Info / Warn / Error 三枚 toggle）
  - 【清空日志】【全部热重载】【打开插件根目录】（用 Tauri opener 打开 `${appDataDir}/plugins`）
- **日志区**：等宽字体，带插件 ID 彩色前缀 + 时间戳；Warn 橙色、Error 红色 + 图标
- **性能区（可选 v2）**：CPU/内存实时小条图；卡死插件一键禁用

### 8.3 插件注入的动态 UI 清单（与真实组件对应）

| 注入位置 | 实际 RD 组件 | UiApi 方法 |
|---|---|---|
| 顶部工具栏 | `Toolbar.tsx` 左/中/右三栏 | `registerToolbarButton` / `remove` |
| 左侧主机树 | `Sidebar.tsx` 分类 header 右键 / 主机右键 / 底部附加区 | `registerSidebarAction` / `remove` |
| 双栏 SFTP 右键菜单 | `FileBrowser.tsx` 右键上下文菜单（local & remote 两侧） | `registerFileContextMenu` / `remove` |
| 右侧信息抽屉 | `RightPanel.tsx`（ServerInfo 所在的右侧面板） | `openRightPanel` / `closeRightPanel` |
| 设置对话框子分组 | `SettingsDialog.tsx` 每个大 Tab 内部 | `registerSettingsSubTab` / `remove` |
| TabBar 右键菜单 | `TabBar.tsx` 标签右键（`关闭左侧/关闭右侧/关闭除此以外`） | v1 先不出；放 v2 需求 |
| 状态栏插件图标 | `StatusBar.tsx` 右侧状态区 | v1 先不出；放 v2 |

### 8.4 异常状态 UI

| 场景 | 视觉表现 |
|---|---|
| 插件加载失败 | 列表行红色「加载失败」标签；hover tooltip 显示错误栈；开关置灰 |
| 运行时异常 | Sandbox iframe 被替换为红框错误页；Toast 警告「插件 xxx 运行异常，已自动停止 UI」；其他插件不受影响 |
| 权限被撤销 | SDK 调用返回 `PERMISSION_DENIED`；弹 Toast 提示并提供跳转权限面板按钮 |
| 互斥冲突 | 详情面板顶部红色条：「与 xxx 插件互斥，请先禁用其中一个」+ 跳转链接 |
| 插件缺少高权限时想调用高权限 | 弹系统确认「插件 xxx 正在请求权限 xxx，是否临时授予？」，临时授予仅在本次启用内有效 |

---

## 九、安全沙箱设计

### 9.1 沙箱选型：iframe（前端） + 受控 postMessage 桥（内核）

> **不采用 deno_core / V8**（避免 20MB+ 二进制膨胀，且会让 Tauri release 体积显著增大）。选择 RD 已经拥有的 **WebView 环境**。

- 每个插件一个 `<iframe sandbox="allow-scripts allow-popups">`（`allow-same-origin` 故意不设，让 iframe 处于 opaque origin，天然跨域隔离 RD 主窗 DOM）
- 插件 SDK `@rd/plugin-sdk` 在 iframe 内部运行，内部包装成：所有 API 调用 → `parent.postMessage({ __rd_plugin_call: true, ... })` → RD 主窗 `PluginSandbox.tsx` 监听 message 事件 → 统一权限校验 → 调用 hostStore / Tauri command / FileBrowser API → 通过 `MessageChannel` 返回结果
- **无 eval 能力**：manifest 声明 `allowEval: true` 时才能打开（v1 强制所有插件 `allowEval: false`）

### 9.2 安全约束清单

#### 1. 权限白名单

- 未声明权限的 SDK 调用 → 统一 `PERMISSION_DENIED`
- 权限校验在**内核（PluginSandbox 桥接层）**执行，iframe 侧 SDK 的权限校验只是客户端快速失败，不可信

#### 2. 文件访问隔离

- 插件文件 API 只能写 `${appDataDir}/plugin-data/${pluginId}/` 目录
- 读取本地其他目录：必须先调 `@tauri-apps/plugin-dialog` 的文件对话框（`UiApi` 封装）；禁止直接 `file://` 访问
- **永久黑名单目录**：`~/.ssh`、`%USERPROFILE%\.ssh`、`${appDataDir}/credentials/`、`/etc`、插件其他 `plugin-data/` 子目录

#### 3. 进程调用拦截

- 插件绝对禁止 `Tauri Command: { shell: open, shell: spawn }`；**所有命令执行只能通过 `ctx.ssh.runCommand(hostId, cmd)` 在远程 SSH 会话内**
- 将来如需本地命令：加权限 `shell.exec`（默认关）+ 命令白名单 + 二次确认

#### 4. 网络限制

- 所有 `network.http` 走内核层 `reqwest` 代理，**阻止内网 RFC1918 网段 + `localhost/127.0.0.1`**
- 域名级黑名单：用户可在「插件调试 → HTTP 黑名单」手动填域名
- 插件无法直接 WebSocket；v2 再议

#### 5. 插件签名校验（Phase 6）

- 官方 RD 插件市场提供 ed25519 签名校验 + 开发者身份
- 本地未签名 `.rdplugin` 安装时弹窗「该插件未通过 RD 官方签名，存在风险，请确认来源」

#### 6. 资源限制

- CPU：沙箱 `iframe` 使用 `requestIdleCallback` 节流；检测 5 秒内不响应 → 判定卡死 → toast 提示并自动禁用
- 内存：`performance.memory.usedJSHeapSize` 轮询（Chrome 有扩展），单插件上限 200MB；超则自动禁用
- 死循环检测：单次 SDK 调用 + `setInterval` 回调运行时长超 200ms 时强制打断

### 9.3 异常隔离 & 降级

- 每个插件 iframe **独立 error boundary**；崩溃只替换该 iframe 的展示为错误页
- 插件 `disable()` 后，SDK 桥接层会：
  - 清除所有事件监听（`bus.offAll(owner)`）
  - 清除插件注册的所有 UI 项
  - 清除 `setInterval` / `setTimeout`（通过 SDK 内部 timer 表）
  - 关闭所有 openRightPanel 抽屉、Toast、Confirm
- 插件卸载：`${appDataDir}/plugin-data/${id}/` 目录 **递归 rm -rf**（`@tauri-apps/plugin-fs::remove_dir_all`）

---

## 十、热加载机制

### 10.1 完整流程（毫秒级生效）

```
插件目录文件变更（manifest/入口 JS/HTML/schema/assets）
        ↓
Rust notify 或前端 chokidar 检测到 change → 500ms 防抖
        ↓
PluginManager.scheduleReload(pluginId)
        ↓
调用旧实例 disable() → 释放 UI + 事件监听 + 定时器 + openRightPanel
        ↓
销毁旧 iframe (removeChild) + 关闭 MessageChannel port
        ↓
重新解析 manifest → 版本/依赖冲突校验
        ↓
创建新 iframe → 注入 postMessage 桥接
        ↓
调用新实例 init(ctx, bus) → 若 enabled=true 调用 enable()
        ↓
完成 → Toast 通知「插件 xxx 已热重载」
```

### 10.2 文件监听

- **监听目录**：`${appDataDir}/plugins/${id}@${version}/`
- **开发模式**：插件开发者在 Settings → 插件 → 开发者控制台勾选「监听插件目录」即可启用；生产模式**默认关闭**
- **触发条件**：manifest.json / config.schema.json / index.html / main.js / assets/ 任何文件变更
- **失败降级**：热加载失败 → 保留旧实例运行；Toast 红色显示错误摘要；开发者控制台打印完整堆栈

### 10.3 注意事项

1. 插件 `disable()` 必须实现完整释放；漏释放会导致「UI 双注册 / 事件双触发」
2. `manifest.json` 的 `hotReload: false` 插件不支持热加载；通常用于：签名插件、内核依赖强的插件
3. 热加载期间插件状态不保留；若插件需跨重载保留，应写入 `ctx.storage`（持久化），重载后读回

---

## 十一、端口转发插件专项规范

> 规范标识：`RD-PortForward-Spec v1.0`
> 依赖：`plugin-sdk v1`
> 所需权限：`tunnel.manage`（极高风险，全局开关可禁）+ `storage.read` + `storage.write`

### 11.1 设计原则

1. **底层内核实现**：TCP 监听 / SSH 通道 forward-tcp / direct-tcpip 完全在 Rust `src-tauri/src/tunnel/*.rs` 实现
2. **插件只做管理层**：规则 CRUD、配置弹窗 UI、自动启停策略、事件联动、导入导出
3. **会话绑定**：隧道生命周期强绑定 SSH 会话；会话关闭/重连/掉线 → **内核强制关闭该会话所有隧道**
4. **禁止后台残留**：绝不允许脱离 SSH 会话的独立 TCP 监听
5. **审计日志**：所有隧道 start / stop / error 均以 `[tunnel:{id}]` 前缀写入 update_log

### 11.2 数据模型（对齐 `types/index.ts` 字段风格）

```typescript
type TunnelMode = 'local' | 'remote' | 'dynamic'

interface TunnelRule {
  id: string                    // uuid v4
  hostId: string                // 注意：SDK / 类型统一用 hostId 而非 serverId（对齐 HostConfig.id）
  mode: TunnelMode
  localAddr: string             // 127.0.0.1 / 0.0.0.0
  localPort: number             // 1-65535
  remoteAddr?: string           // local / remote 模式必填
  remotePort?: number           // local / remote 模式必填
  autoStart: boolean            // connection:success 后自动 start
  tags?: string[]
  comment?: string
  createdAt: number
}

interface TunnelStatus {
  tunnelId: string
  running: boolean
  pid?: number                  // SSH channel id（调试用）
  error?: string
  boundHostId?: string          // 实际绑定的 SSH 会话 hostId（与 rule.hostId 一致）
  acceptedConns: number         // 累计接受多少 TCP 连接（审计用）
  startTimeMs?: number
}
```

### 11.3 内核强制校验（插件调 SDK 时自动执行）

1. `localPort ∈ [1, 65535]`
2. `localAddr` 为合法 IPv4/IPv6；`0.0.0.0` 必须二次确认「监听所有网卡，其他设备可访问此端口」
3. `local` / `remote` 模式必须提供 `remoteAddr` + `remotePort`
4. `dynamic` 模式不得带 `remoteAddr / remotePort`
5. `hostId` 必须在 `HostConfigSafe` 列表存在
6. `remote` 模式：若全局开关「禁止远程转发」开启 → 直接 `REMOTE_FORBIDDEN`
7. 同一 hostId 下 `localAddr + localPort` 不得重复

### 11.4 TunnelApi

```typescript
interface TunnelApi {
  // ===== 规则 CRUD（持久化） =====
  createRule(rule: Omit<TunnelRule, 'id' | 'createdAt'>): Promise<TunnelRule>
  removeRule(tunnelId: string): Promise<void>
  listRules(hostId?: string): Promise<TunnelRule[]>
  updateRule(tunnelId: string, patch: Partial<TunnelRule>): Promise<TunnelRule>

  // ===== 运行控制 =====
  startTunnel(tunnelId: string): Promise<TunnelStatus>   // 前置：host.state === connected
  stopTunnel(tunnelId: string): Promise<void>            // 内核主动 stop → 同时 emit tunnel:stop
  listStatus(hostId?: string): Promise<TunnelStatus[]>

  // ===== 导入导出（.rd-tunnels.json v1） =====
  exportRules(filter?: { hostIds?: string[]; mode?: TunnelMode }): Promise<string>
  importRules(json: string, opts?: {
    onConflict?: 'skip' | 'overwrite' | 'rename'
    defaultHostId?: string
  }): Promise<{ imported: number; skipped: number; errors: { id?: string; reason: string }[] }>
}
```

### 11.5 错误码

```typescript
enum TunnelErrorCode {
  RULE_NOT_FOUND = 'RULE_NOT_FOUND',
  HOST_NOT_AVAILABLE = 'HOST_NOT_AVAILABLE',      // 主机无 SSH 会话（未连接 / reconnecting）
  HOST_RECONNECTING = 'HOST_RECONNECTING',        // 自动重连期间禁止 start（等重连成功后 autoStart）
  PORT_IN_USE = 'PORT_IN_USE',
  PORT_INVALID = 'PORT_INVALID',
  ADDR_INVALID = 'ADDR_INVALID',
  REMOTE_FORBIDDEN = 'REMOTE_FORBIDDEN',
  PERMISSION_DENIED = 'PERMISSION_DENIED',
  LISTEN_ON_ALL_NEEDS_CONFIRM = 'LISTEN_ON_ALL_NEEDS_CONFIRM',  // 0.0.0.0 未确认
  SSH_CHANNEL_ERROR = 'SSH_CHANNEL_ERROR',
}
```

### 11.6 隧道事件（与第五章对齐）

```
tunnel:start       [tunnelId, status]
tunnel:stop        [tunnelId, reason: 'manual' | 'host-close' | 'host-reconnecting' | 'uninstall']
tunnel:error       [tunnelId, code, msg]
tunnel:connection  [tunnelId, peerAddr, peerPort]  → 每次新 TCP 客户端接入（审计）
```

### 11.7 与 SSH 会话 / 重连联动

1. `connection:success(hostId, result)` → 插件扫描该 hostId 下 `autoStart = true` 的规则，并行 `startTunnel()`
2. `connection:reconnecting(hostId, meta)` → 内核自动 stop 该 hostId 所有隧道；emit `tunnel:stop`，reason=host-reconnecting
3. `connection:reconnect-success(hostId)` → 插件重新 autoStart（无需内核负责，因为重启后的 channel id 会变）
4. `connection:close(hostId, 'passive' | 'user')` → 内核自动 stop 所有隧道（无论是什么 close 原因）

> 重要：stop 行为是**内核层**行为，不是插件责任。即使插件崩了也能防止端口残留。

### 11.8 导入导出 `.rd-tunnels.json`

```json
{
  "$schema": "https://raw.githubusercontent.com/zhangOranges/RD/main/schemas/rd-tunnels-v1.schema.json",
  "specVersion": "1.0",
  "exportTime": "2026-08-15T12:00:00Z",
  "exportedBy": "rd-app 0.1.89",
  "rules": [
    {
      "id": "9c2e9f...",
      "hostId": "a7b81f...",
      "mode": "local",
      "localAddr": "127.0.0.1",
      "localPort": 33060,
      "remoteAddr": "127.0.0.1",
      "remotePort": 3306,
      "autoStart": true,
      "comment": "MySQL 转发"
    }
  ]
}
```

### 11.9 插件 manifest 约定

```json
{
  "id": "port-forward-manager",
  "name": "端口转发管理器",
  "version": "1.0.0",
  "apiVersion": "v1",
  "category": "tunnel",
  "permissions": ["tunnel.manage", "storage.read", "storage.write", "ui.inject-menu", "ui.dialog"],
  "minRdVersion": "0.1.89",
  "hotReload": true
}
```

### 11.10 官方端口转发插件 UI 交互约定

1. **入口**：`ui.registerToolbarButton({ id: 'port-forward-open', label: '端口转发', icon: 'Network', group: 'center' })`
2. **弹窗表格列**：状态圆点（绿/黄/灰）+ 模式胶囊（local/remote/dynamic，不同颜色）+ 绑定主机名（含 icon）+ 本地地址:端口 → 远程地址:端口 + autoStart 开关 + 操作（启动/停止/编辑/删除）
3. **新增表单顺序**：绑定主机 → 转发模式 → 本地监听地址 → 本地端口 → 远程地址/端口（conditional）→ autoStart → tags → 备注
4. **高危提示**：
   - `mode = remote`：红色条「远程转发会把服务器端口暴露到本地网络，请谨慎使用。可在设置 → 插件全局关闭所有远程转发。」
   - `localAddr = 0.0.0.0`：确认弹窗「监听所有网卡，其他局域网设备可访问该端口，是否继续？」
5. **导入导出**：顶部菜单按钮「导入 .rd-tunnels.json」/「导出为 .rd-tunnels.json」

### 11.11 安全约束清单

- 隧道绑定 SSH 会话，会话关闭 → 内核强制关闭隧道
- `-R` 远程转发受全局开关控制
- `0.0.0.0` 监听二次确认
- 插件卸载时 → 内核停止其所有 tunnel（即使插件自身 disable 没写好）
- 插件绝不允许绕过 `TunnelApi` 自己 bind TCP

---

## 十二、落地迭代路线图（贴合 RD 当前版本节奏）

### Phase 1：基础骨架（预计 RD v0.1.90 引入）

- [ ] 新建 `@rd/plugin-sdk` 类型包（单 `index.ts`，类型对齐「第十三章」）
- [ ] Rust `plugin/manifest.rs`：JSON 解析 + 强制 schema 校验（`ajv` 或 jsonschema crate）
- [ ] 前端 `PluginSandbox.tsx`：iframe + postMessage 桥接雏形
- [ ] 前端 `pluginStore.ts`：扫描 / 列表 / 启用禁用 / 持久化
- [ ] SettingsDialog：在「调试」与「关于」之间插入「插件」Tab（含 3 个子 Tab 的空框架）
- [ ] 生命周期：init / enable / disable / uninstall

### Phase 2：事件总线 + 基础 SDK（v0.1.91）

- [ ] EventBus Bridge：内核已有的 hostStore 事件 → 分发到各插件沙箱（含 reconnect 6 事件）
- [ ] storage API：隔离 `plugin-data/${id}/`；config.schema.json 自动渲染
- [ ] ui.notify / confirm / prompt / Toolbar 按钮注册
- [ ] log API：`[plugin:${id}]` 前缀复用 update_log
- [ ] 权限校验基础框架：声明制 + 安装确认 + 4 高危权限红标
- [ ] 调试控制台日志视图（插件筛选）

### Phase 3：业务 SDK（v0.1.92）

- [ ] `SshApi.runCommand`（复用连接池 + 分类友好错误）
- [ ] `SftpApi`：list / stat / mkdir / remove / rename + upload/download 接入 transferQueue + 临时文件协议
- [ ] `ServerApi`：listAll（脱敏 HostConfigSafe）/ add / update / remove + getConnectionState
- [ ] `ThemeApi`：主题事件 + CSS var 同步到 iframe
- [ ] 插件管理 Tab 的权限撤销功能 + 3 个全局开关
- [ ] 凭据打码（maskMode）自动对插件展示生效

### Phase 4：热重载 + 安装（v0.1.93）

- [ ] 文件监听热重载（Rust notify + 前端 500ms 防抖）
- [ ] `.rdplugin` zip 打包格式 + 双击安装（Tauri 协议 `rd-plugin://`）
- [ ] 本地拖拽安装 + 安装流程弹窗（风险确认 + 权限清单 + 我理解风险复选框）
- [ ] 沙箱卡死检测 & 资源限制（500ms watchdog）

### Phase 5：端口转发专项 + 官方插件（v0.1.94）

- [ ] Rust 侧 `tunnel/*.rs` 底层实现：local/remote/dynamic
- [ ] `TunnelApi` + 隧道事件
- [ ] 官方端口转发插件（完全遵守第十一章）
- [ ] `.rd-tunnels.json` 导入导出
- [ ] 官方云主机导入插件模板仓库（阿里云 / 腾讯云参考实现）

### Phase 6：生态完善（v1.0 长期）

- [ ] GitHub Pages 插件市场（基于 GitHub Issue 列表 + 官方审核 PR）
- [ ] 插件签名（ed25519）+ 未签名警告
- [ ] 官方插件审核流程 + "RD 官方插件"徽章
- [ ] 插件开发者文档站（Vitepress）+ 模板脚手架（`npm create rd-plugin@latest`）
- [ ] 插件跨设备配置同步（通过加密文件到用户云盘）

---

## 十三、附录：完整 SDK 类型定义

```typescript
/**
 * RD Plugin SDK v1
 * 插件开发者仅依赖此包；严禁 import RD 内部模块。
 *
 * 类型 100% 对齐 RD 主工程 src/types/index.ts 与各 store。
 */

// ============================================================
// 1. Manifest 插件元数据
// ============================================================
export interface PluginManifest {
  id: string
  name: string
  version: string
  apiVersion: 'v1'
  author: string
  description: string
  category:
    | 'connection'
    | 'command'
    | 'file'
    | 'terminal'
    | 'monitor'
    | 'notify'
    | 'tunnel'
    | 'other'
  entry: string
  indexHtml: string
  configSchema?: string
  icon?: string
  permissions: PluginPermission[]
  conflict?: string[]
  requires?: string[]
  minRdVersion: string
  hotReload: boolean
}

export type PluginPermission =
  | 'network.http'
  | 'storage.read'
  | 'storage.write'
  | 'file.local.read'
  | 'file.local.write'
  | 'server.read'
  | 'server.write'
  | 'server.manage'
  | 'ssh.run'
  | 'sftp.operate'
  | 'ui.notification'
  | 'ui.dialog'
  | 'ui.inject-menu'
  | 'theme.read'
  | 'tunnel.manage'

// ============================================================
// 2. 复用 RD 内置类型（src/types/index.ts）
// ============================================================
export interface HostConfig {
  id: string
  name: string
  host: string
  port: number
  username: string
  auth_type: 'password' | 'key'
  remember_dir: boolean
  remark: string
  category_id: string
  path_cache_id?: string
}

export interface HostConfigSafe extends HostConfig {
  has_password: boolean
  has_private_key: boolean
  // 注意：永远不返回 password / private_key 原文
}

export interface CategoryConfig {
  id: string
  name: string
  order: number
}

export type ConnectionState = 'disconnected' | 'connecting' | 'connected' | 'reconnecting'

export interface ReconnectMeta {
  attempt: number
  nextDelayMs: number
  nextAt: number
}

export interface ConnectResult {
  home_dir: string
  fingerprint: string
}

export type TransferKind = 'upload' | 'download'
export type TransferStatus = 'queued' | 'running' | 'completed' | 'error' | 'canceled'

export interface TransferTask {
  id: string
  kind: TransferKind
  hostId: string
  name: string
  remotePath: string
  localPath: string
  status: TransferStatus
  bytesTransferred: number
  totalBytes: number
  speedBytesPerSec: number
  startedAt: number
  finishedAt: number
  errorMessage?: string
}

export interface SftpFile {
  name: string
  path: string
  isDir: boolean
  size: number
  modifiedAt: number
  permissions?: string
}

export interface FriendlyFailure {
  kind: 'network' | 'auth' | 'config' | 'unknown'
  headline: string
  suggestion: string
}

export interface CommandResult {
  success: boolean
  output: string
  exitCode: number
}

export interface BatchResult {
  batchId: string
  total: number
  success: number
  fail: number
  results: Record<string, CommandResult>
}

export type ToastKind = 'info' | 'success' | 'error' | 'warning'

// ============================================================
// 3. 端口转发（第十一章）
// ============================================================
export type TunnelMode = 'local' | 'remote' | 'dynamic'

export interface TunnelRule {
  id: string
  hostId: string
  mode: TunnelMode
  localAddr: string
  localPort: number
  remoteAddr?: string
  remotePort?: number
  autoStart: boolean
  tags?: string[]
  comment?: string
  createdAt: number
}

export interface TunnelStatus {
  tunnelId: string
  running: boolean
  pid?: number
  error?: string
  boundHostId?: string
  acceptedConns: number
  startTimeMs?: number
}

export enum TunnelErrorCode {
  RULE_NOT_FOUND = 'RULE_NOT_FOUND',
  HOST_NOT_AVAILABLE = 'HOST_NOT_AVAILABLE',
  HOST_RECONNECTING = 'HOST_RECONNECTING',
  PORT_IN_USE = 'PORT_IN_USE',
  PORT_INVALID = 'PORT_INVALID',
  ADDR_INVALID = 'ADDR_INVALID',
  REMOTE_FORBIDDEN = 'REMOTE_FORBIDDEN',
  PERMISSION_DENIED = 'PERMISSION_DENIED',
  LISTEN_ON_ALL_NEEDS_CONFIRM = 'LISTEN_ON_ALL_NEEDS_CONFIRM',
  SSH_CHANNEL_ERROR = 'SSH_CHANNEL_ERROR',
}

// ============================================================
// 4. 事件总线类型（第五章全量）
// ============================================================
export type RdEventMap = {
  // 连接 & 重连
  'connection:before-connect': [host: HostConfig]
  'connection:success': [hostId: string, result: ConnectResult]
  'connection:error': [hostId: string, friendly: FriendlyFailure, rawError: string]
  'connection:close': [hostId: string, reason: 'user' | 'passive']
  'connection:reconnecting': [hostId: string, meta: ReconnectMeta]
  'connection:reconnect-attempt': [hostId: string, attempt: number, nextDelayMs: number]
  'connection:reconnect-success': [hostId: string, result: ConnectResult]
  'connection:reconnect-failed': [hostId: string, attempt: number, nextAt: number]
  'connection:reconnect-aborted': [hostId: string, reason: 'canceled' | 'timeout' | 'user-disconnected']
  'connection:batch-start': [batchId: string, hostIds: string[]]
  'connection:batch-finish': [batchId: string, results: BatchResult]

  // 终端
  'terminal:new-tab': [terminalId: string, hostId?: string]
  'terminal:output': [terminalId: string, data: Uint8Array]
  'terminal:input': [terminalId: string, input: string]
  'terminal:resize': [terminalId: string, cols: number, rows: number]
  'terminal:closed': [terminalId: string]
  'terminal:theme-change': [themeId: string, palette: Record<string, string>]

  // SFTP & 传输
  'sftp:list-finish': [hostId: string, path: string, files: SftpFile[]]
  'sftp:navigate': [hostId: string, from: string, to: string]
  'sftp:upload-start': [task: TransferTask]
  'sftp:upload-progress': [taskId: string, bytes: number, total: number, speedBps: number]
  'sftp:upload-finish': [taskId: string, status: TransferStatus, errorMsg?: string]
  'sftp:download-start': [task: TransferTask]
  'sftp:download-progress': [taskId: string, bytes: number, total: number, speedBps: number]
  'sftp:download-finish': [taskId: string, status: TransferStatus, errorMsg?: string]
  'sftp:file-delete': [hostId: string, side: 'local' | 'remote', path: string, isDir: boolean]
  'sftp:file-rename': [hostId: string, side: 'local' | 'remote', from: string, to: string]

  // 主机 & 分类 CRUD
  'host:added': [host: HostConfigSafe]
  'host:updated': [hostId: string, patch: Partial<HostConfigSafe>]
  'host:removed': [hostId: string]
  'category:added': [category: CategoryConfig]
  'category:updated': [category: CategoryConfig]
  'category:removed': [category: CategoryConfig]

  // UI & 主题 & 设置
  'ui:theme-change': [themeId: string]
  'ui:mask-mode-change': [masked: boolean]
  'ui:settings-change': [changed: Record<string, unknown>]
  'ui:tab-change': [tabId: string, tabType: 'sftp' | 'terminal']
  'ui:notification': [kind: ToastKind, message: string]
  'ui:window-blur': [windowId: string]
  'ui:window-focus': [windowId: string]

  // 隧道
  'tunnel:start': [tunnelId: string, status: TunnelStatus]
  'tunnel:stop': [tunnelId: string, reason: 'manual' | 'host-close' | 'host-reconnecting' | 'uninstall']
  'tunnel:error': [tunnelId: string, code: TunnelErrorCode, msg: string]
  'tunnel:connection': [tunnelId: string, peerAddr: string, peerPort: number]

  // 自动更新 & 系统
  'updater:check-finished': [found: boolean, version?: string]
  'updater:downloading': [bytes: number, total: number]
  'updater:ready-to-install': [version: string, sigVerified: boolean]
  'log:write': [level: 'info' | 'warn' | 'error', tag: string, msg: string]
  'config:sync': [timestamp: number]
}

export interface EventBus {
  on<T extends keyof RdEventMap>(
    event: T,
    handler: (...args: RdEventMap[T]) => void,
    owner?: object,
  ): void
  off<T extends keyof RdEventMap>(
    event: T,
    handler: (...args: RdEventMap[T]) => void,
  ): void
  offAll(owner: object): void
  emit<T extends keyof RdEventMap>(event: T, ...args: RdEventMap[T]): void
}

// ============================================================
// 5. UI API（与 RD 组件槽位一一对应）
// ============================================================
export interface ToolbarButtonOption {
  id: string
  label: string
  icon?: string
  tooltip?: string
  group?: 'left' | 'center' | 'right'
  onClick: () => void | Promise<void>
}

export interface SidebarActionOption {
  id: string
  label: string
  icon?: string
  location: 'category-header' | 'host-item-menu' | 'sidebar-footer'
  onClick: (ctx: { categoryId?: string; hostId?: string }) => void
}

export interface FileContextMenuOption {
  id: string
  label: string
  icon?: string
  scope: 'local' | 'remote' | 'both'
  kinds: ('file' | 'folder' | 'empty')[]
  onClick: (ctx: {
    hostId?: string
    side: 'local' | 'remote'
    path: string
    selectedPaths: string[]
  }) => void
}

export interface SettingsSubTabOption {
  id: string
  label: string
  icon?: string
  group:
    | 'general'
    | 'terminal'
    | 'theme'
    | 'update'
    | 'shortcut'
    | 'debug'
    | 'plugins'
    | 'about'
  render: 'schema'
}

export interface RightPanelOption {
  id: string
  label: string
  icon?: string
  render: 'schema'
}

export interface UiApi {
  registerToolbarButton(opt: ToolbarButtonOption): void
  removeToolbarButton(id: string): void
  registerSidebarAction(opt: SidebarActionOption): void
  removeSidebarAction(id: string): void
  registerFileContextMenu(opt: FileContextMenuOption): void
  removeFileContextMenu(id: string): void
  registerSettingsSubTab(opt: SettingsSubTabOption): void
  removeSettingsSubTab(id: string): void
  openRightPanel(opt: RightPanelOption): void
  closeRightPanel(id: string): void
  notify(
    title: string,
    message: string,
    kind?: ToastKind,
    durationMs?: number,
  ): void
  confirm(message: string, title?: string): Promise<boolean>
  prompt(message: string, defaultValue?: string, title?: string): Promise<string | null>
  openPluginConfig(pluginId?: string): void
  focusHost(hostId: string): void
  focusTerminal(terminalId: string): void
}

// ============================================================
// 6. Storage / SSH / SFTP / Server / HTTP / Theme / Tunnel
// ============================================================
export interface PluginStorage {
  set(key: string, value: unknown): Promise<void>
  get<T = unknown>(key: string): Promise<T | null>
  remove(key: string): Promise<void>
  removeAll(): Promise<void>
  writeFile(name: string, content: Uint8Array | string): Promise<void>
  readFile(name: string): Promise<Uint8Array | null>
  listFiles(): Promise<string[]>
}

export interface SshApi {
  runCommand(
    hostId: string,
    cmd: string,
    opt?: { timeoutMs?: number; cwd?: string },
  ): Promise<CommandResult>
  onTerminalOutput(
    terminalId: string,
    listener: (data: Uint8Array) => void,
  ): () => void
}

export interface TransferHandle {
  taskId: string
  abort(): Promise<void>
  onProgress(listener: (b: number, total: number, speedBps: number) => void): void
  finished(): Promise<'completed' | 'error' | 'canceled'>
}

export interface SftpApi {
  list(hostId: string, remotePath: string): Promise<SftpFile[]>
  stat(hostId: string, remotePath: string): Promise<SftpFile | null>
  mkdir(hostId: string, remotePath: string): Promise<void>
  remove(hostId: string, remotePath: string, recursive?: boolean): Promise<void>
  rename(hostId: string, from: string, to: string): Promise<void>
  readText(hostId: string, remotePath: string): Promise<string>
  writeText(hostId: string, remotePath: string, content: string): Promise<void>
  upload(hostId: string, localPath: string, remotePath: string): TransferHandle
  download(hostId: string, remotePath: string, localPath: string): TransferHandle
}

export interface ServerApi {
  listAll(): Promise<HostConfigSafe[]>
  get(hostId: string): Promise<HostConfigSafe | null>
  add(item: Omit<HostConfig, 'id'> & {
    password?: string | null
    private_key?: string | null
  }): Promise<HostConfig>
  update(hostId: string, patch: Partial<Omit<HostConfig, 'id'>> & {
    password?: string | null
    private_key?: string | null
  }): Promise<HostConfig>
  remove(hostId: string): Promise<void>
  listCategories(): Promise<CategoryConfig[]>
  addCategory(name: string, order?: number): Promise<CategoryConfig>
  updateCategory(id: string, patch: Partial<CategoryConfig>): Promise<CategoryConfig>
  removeCategory(id: string): Promise<void>
  getConnectionState(hostId: string): Promise<{
    state: ConnectionState
    reconnectMeta?: ReconnectMeta
    homeDir?: string
    fingerprint?: string
  }>
  connect(hostId: string): Promise<void>
  disconnect(hostId: string, options?: { suppressReconnect?: boolean }): Promise<void>
  cancelReconnect(hostId: string): Promise<void>
}

export type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH'

export interface HttpApi {
  request<T = unknown>(
    url: string,
    opt?: {
      method?: HttpMethod
      headers?: Record<string, string>
      body?: unknown
      timeoutMs?: number
    },
  ): Promise<T>
  get<T = unknown>(url: string, headers?: Record<string, string>): Promise<T>
  post<T = unknown>(
    url: string,
    body?: unknown,
    headers?: Record<string, string>,
  ): Promise<T>
}

export interface ThemeApi {
  getCurrent(): Promise<{
    id: string
    name: string
    cssVars: Record<string, string>
  }>
  onChange(listener: (themeId: string) => void): () => void
  syncToIframe(win: Window): void
}

export interface TunnelApi {
  createRule(rule: Omit<TunnelRule, 'id' | 'createdAt'>): Promise<TunnelRule>
  removeRule(tunnelId: string): Promise<void>
  listRules(hostId?: string): Promise<TunnelRule[]>
  updateRule(tunnelId: string, patch: Partial<TunnelRule>): Promise<TunnelRule>
  startTunnel(tunnelId: string): Promise<TunnelStatus>
  stopTunnel(tunnelId: string): Promise<void>
  listStatus(hostId?: string): Promise<TunnelStatus[]>
  exportRules(filter?: { hostIds?: string[]; mode?: TunnelMode }): Promise<string>
  importRules(
    json: string,
    opts?: {
      onConflict?: 'skip' | 'overwrite' | 'rename'
      defaultHostId?: string
    },
  ): Promise<{ imported: number; skipped: number; errors: { id?: string; reason: string }[] }>
}

// ============================================================
// 7. RDContext & BasePlugin（最顶层入口）
// ============================================================
export interface RDContext {
  readonly pluginId: string
  readonly manifest: PluginManifest

  ui: UiApi
  storage: PluginStorage
  ssh: SshApi
  sftp: SftpApi
  server: ServerApi
  http: HttpApi
  tunnel: TunnelApi
  theme: ThemeApi

  log: {
    info(msg: string): void
    warn(msg: string): void
    error(msg: string): void
  }
}

export abstract class BasePlugin {
  public manifest!: PluginManifest
  abstract init(ctx: RDContext, bus: EventBus): Promise<void>
  abstract enable(): Promise<void>
  abstract disable(): Promise<void>
  abstract uninstall(): Promise<void>
  abstract onConfigChange(newConfig: Record<string, unknown>): Promise<void>
}

export type PluginClass = new () => BasePlugin
```

---

**文档结束**

> 本文档为 RD 插件系统 v1.0 规范，100% 对齐 RD 当前真实代码结构：
> `package.json: version 0.1.89`、`src/types/index.ts` 类型、`hostStore.ts` 连接状态机 & 自动重连、
> SettingsDialog 7 Tab 顺序（插件插入第 7 位）、`utils/log.ts` 日志桥接、CI 三平台、凭据安全策略。
> 后续迭代请按 phase 计划推进并在 CHANGELOG.md 中记录。
