# RD 插件系统设计文档 v1.0

> 项目：RD - Linux可视化SSH客户端
> 仓库：https://github.com/zhangOranges/RD
> 技术栈：Tauri 2.0 + Vue3 + Rust
> 文档版本：v1.0
> 更新日期：2026-08-15

---

## 目录

1. [概述](#一概述)
2. [插件系统整体架构](#二插件系统整体架构)
3. [插件标准规范](#三插件标准规范)
4. [插件SDK API 契约](#四插件sdk-api-契约)
5. [事件总线规范](#五事件总线规范)
6. [权限系统规范](#六权限系统规范)
7. [插件管理器设计](#七插件管理器设计)
8. [插件UI界面设计](#八插件ui界面设计)
9. [安全沙箱设计](#九安全沙箱设计)
10. [热加载机制](#十热加载机制)
11. [端口转发插件专项规范](#十一端口转发插件专项规范)
12. [落地迭代路线图](#十二落地迭代路线图)
13. [附录：完整SDK类型定义](#十三附录完整sdk类型定义)

---

## 一、概述

### 1.1 设计目标

RD 是一款面向服务器批量运维、终端管理的桌面 SSH 工具。插件系统的核心目标：

- **低耦合**：插件不侵入主程序代码，主程序只暴露标准接口
- **热插拔**：安装、启用、禁用、卸载无需重启 RD 客户端
- **可扩展**：覆盖 SSH 运维全流程能力（连接、命令、文件、终端、监控、通知）
- **安全可控**：沙箱隔离 + 权限白名单，防止第三方插件高危操作
- **标准化**：统一数据模型、API、事件、存储格式，第三方开发者可遵循规范开发

### 1.2 插件可扩展的能力范围

| 分类 | 可扩展场景 |
|------|-----------|
| **连接层** | 新协议（Telnet/RDP）、密钥管理、云服务器自动导入（阿里云/腾讯云ECS） |
| **会话生命周期** | 连接前校验、登录钩子、断线重连策略、会话标签分组 |
| **批量命令** | 自定义批量运维脚本、命令模板、执行结果解析、告警推送（钉钉/企业微信） |
| **文件传输** | 批量上传下载、压缩解压、OSS/COS 互通、本地目录同步 |
| **终端 UI** | 自定义终端主题、字体、快捷键、水印、日志格式化 |
| **运维工具** | 服务器负载监控、磁盘巡检、容器/Docker 管理、数据库快捷登录 |
| **系统增强** | 镜像源管理、日志导出、配置同步、备份恢复、自动化定时任务 |
| **通知** | 执行完成/异常告警、消息推送、邮件报告 |
| **网络隧道** | SSH 端口转发（本地/远程/SOCKS5）管理 |

### 1.3 设计核心原则

1. **单一职责**：一个插件只解决一类运维场景，禁止大杂烩插件
2. **松耦合**：主程序只提供标准接口，插件不能直接 import 主程序内部业务类
3. **隔离安全**：插件沙箱隔离，限制高危系统调用，权限白名单管控
4. **热加载**：安装/启用/禁用无需重启 RD
5. **向后兼容**：插件 API 版本化，旧插件在新版客户端兼容运行
6. **可配置**：每个插件独立配置面板，持久化存储用户参数

---

## 二、插件系统整体架构

### 2.1 三层架构

```
┌─────────────────────────────────────────────────────┐
│                  RD 主程序（内核层）                  │
│  SSH会话池 / SFTP / 终端渲染 / 服务器管理 / 事件总线   │
└───────────────────────┬─────────────────────────────┘
                        │ 标准插件API、事件总线、上下文
┌───────────────────────▼─────────────────────────────┐
│                插件管理器（调度层）                   │
│  加载/注册/生命周期/权限校验/管理面板/热重载/沙箱     │
└───────────────────────┬─────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────┐
│                第三方插件（业务扩展层）               │
│  端口转发 / 云主机导入 / 钉钉告警 / 监控巡检 / ...    │
└─────────────────────────────────────────────────────┘
```

### 2.2 各层职责

#### 内核层（RD 主程序）
- **上下文 Context**：对外开放只读运维数据（服务器信息、会话列表、终端流等）
- **全局事件总线 EventBus**：全生命周期钩子事件（核心扩展点）
- **内置工具 SDK**：封装 SSH、SFTP、进程调用、本地文件、网络请求、弹窗、表单组件
- **权限控制中心**：文件读写、网络访问、执行本地命令、读取密钥等权限开关

#### 调度层（插件管理器）
- 插件扫描与加载
- 生命周期调度（安装→加载→初始化→启用→运行→禁用→卸载）
- 依赖解析与冲突检测
- 沙箱隔离执行
- 统一管理 UI

#### 扩展层（第三方插件）
- 每个插件是独立包，结构标准化
- 完全隔离，不污染主程序代码
- 只能通过 SDK API 与主程序交互

---

## 三、插件标准规范

### 3.1 插件目录结构

每个插件为独立文件夹，结构如下：

```
rd-plugin-xxx/
├─ manifest.json          # 插件元数据（必填，加载入口）
├─ main.ts / main.js      # 插件入口，实现标准 Plugin 基类
├─ config.schema.json     # 插件配置表单结构（自动生成配置面板）
├─ assets/                # 图标、样式、静态资源
│  └─ icon.svg
├─ hooks/                 # 各生命周期钩子实现（可选）
├─ ui/                    # 自定义弹窗、侧边面板组件（可选）
└─ README.md              # 使用说明、兼容版本、权限说明
```

### 3.2 manifest.json 规范

插件身份定义文件，加载入口，必填。

```json
{
  "id": "cloud-ecs-import",
  "name": "云服务器批量导入",
  "version": "1.0.0",
  "apiVersion": "v1",
  "author": "zhangOranges",
  "description": "一键导入阿里云/腾讯云ECS到服务器列表",
  "category": "connection",
  "entry": "./main.ts",
  "configSchema": "./config.schema.json",
  "permissions": ["network.http", "storage.read", "storage.write"],
  "conflict": [],
  "requires": [],
  "icon": "./assets/icon.svg",
  "minRdVersion": "1.6.0",
  "hotReload": true
}
```

#### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | ✅ | 全局唯一ID，kebab-case，不可重复 |
| `name` | string | ✅ | 插件显示名称 |
| `version` | string | ✅ | 插件版本号，语义化版本 |
| `apiVersion` | string | ✅ | 依赖 RD 插件 API 版本，用于兼容校验 |
| `author` | string | ✅ | 作者 |
| `description` | string | ✅ | 简短描述 |
| `category` | string | ✅ | 分类：`connection` / `command` / `file` / `terminal` / `monitor` / `notify` / `other` |
| `entry` | string | ✅ | 入口文件相对路径 |
| `configSchema` | string | ❌ | 配置表单 schema 相对路径 |
| `permissions` | string[] | ✅ | 所需权限列表 |
| `conflict` | string[] | ❌ | 互斥插件 ID 列表 |
| `requires` | string[] | ❌ | 依赖其他插件 ID 列表 |
| `icon` | string | ❌ | 图标相对路径 |
| `minRdVersion` | string | ✅ | 最低支持 RD 客户端版本 |
| `hotReload` | boolean | ✅ | 是否支持热加载 |

### 3.3 插件生命周期

所有插件必须继承 `BasePlugin` 基类，实现 5 个生命周期方法：

| 生命周期 | 触发时机 | 职责 |
|---------|---------|------|
| `init(ctx, bus)` | 插件加载完成 | 保存上下文、注册事件监听，**不注册UI** |
| `enable()` | 用户启用插件 | 注册按钮、菜单、侧边栏入口 |
| `disable()` | 用户禁用插件 | 注销所有UI、事件监听，释放资源 |
| `uninstall()` | 用户卸载插件 | 清理本地缓存、配置文件 |
| `onConfigChange(config)` | 用户修改配置 | 重载插件逻辑 |

#### 标准入口示例

```typescript
import { BasePlugin, RDContext, EventBus } from '@rd/plugin-sdk'

export default class DemoPlugin extends BasePlugin {
  private ctx!: RDContext
  private bus!: EventBus

  async init(ctx: RDContext, bus: EventBus) {
    this.ctx = ctx
    this.bus = bus
    this.registerEvents()
  }

  async enable() {
    this.ctx.ui.registerTopBarButton({
      id: 'demo-btn',
      text: 'Demo按钮',
      onClick: () => this.openPanel()
    })
  }

  async disable() {
    this.ctx.ui.removeTopBarButton('demo-btn')
    this.bus.offAll(this)
  }

  async uninstall() {
    await this.ctx.storage.removeAll()
  }

  async onConfigChange(newConfig: Record<string, unknown>) {
    // 重载配置
  }

  private registerEvents() {
    this.bus.on('connection:success', (serverId) => {
      this.ctx.log.info(`服务器连接成功: ${serverId}`)
    })
  }

  private openPanel() {
    this.ctx.ui.openPluginConfig(this.manifest.id)
  }
}
```

### 3.4 配置表单规范（config.schema.json）

插件配置面板根据 `config.schema.json` 自动渲染，插件不需要写 UI 组件。

使用 JSON Schema 标准子集，支持以下类型：

| 类型 | 渲染组件 |
|------|---------|
| `string` | 输入框 |
| `string` + `format: "password"` | 密码输入框 |
| `string` + `format: "textarea"` | 文本域 |
| `number` | 数字输入框 |
| `boolean` | 开关 |
| `string` + `enum` | 下拉选择 |

#### 示例

```json
{
  "type": "object",
  "title": "钉钉告警配置",
  "properties": {
    "webhook": {
      "type": "string",
      "title": "Webhook地址",
      "description": "钉钉机器人webhook地址",
      "format": "password"
    },
    "enableNotify": {
      "type": "boolean",
      "title": "开启批量执行通知",
      "default": true
    },
    "notifyType": {
      "type": "string",
      "title": "通知时机",
      "enum": ["总是", "仅失败", "仅成功"],
      "default": "总是"
    }
  }
}
```

### 3.5 插件打包格式

- 打包格式：`.rdplugin`（zip 压缩包）
- 内容：插件目录全部文件
- 安装方式：本地拖拽安装 / 插件商店在线安装

---

## 四、插件SDK API 契约

### 4.1 RDContext（插件上下文）

插件通过 `RDContext` 访问 RD 提供的全部能力。

```typescript
interface RDContext {
  readonly pluginId: string
  readonly manifest: PluginManifest

  ui: UiApi
  storage: PluginStorage
  ssh: SshApi
  sftp: SftpApi
  server: ServerApi
  http: HttpApi
  tunnel: TunnelApi

  log: {
    info(msg: string): void
    warn(msg: string): void
    error(msg: string): void
  }
}
```

### 4.2 UI API

插件注册 UI 元素的统一入口，**禁止插件直接操作 DOM**。

```typescript
interface TopBarButtonOption {
  id: string
  text: string
  icon?: string
  onClick: () => void
}

interface SettingItemOption {
  group: string
  name: string
  onClick: () => void
}

interface UiApi {
  // 顶部工具栏按钮
  registerTopBarButton(opt: TopBarButtonOption): void
  removeTopBarButton(id: string): void

  // 设置页菜单项
  registerSettingItem(opt: SettingItemOption): void
  removeSettingItem(id: string): void

  // 打开插件配置弹窗
  openPluginConfig(pluginId: string): void

  // 系统通知 toast
  notify(title: string, msg: string, type: 'success' | 'warn' | 'error' | 'info'): void

  // 确认对话框
  confirm(message: string): Promise<boolean>

  // 输入对话框
  prompt(message: string, defaultValue?: string): Promise<string | null>
}
```

### 4.3 存储 API（插件隔离存储）

每个插件拥有独立存储空间，相互隔离，前缀自动隔离。

```typescript
interface PluginStorage {
  set(key: string, value: unknown): Promise<void>
  get<T = unknown>(key: string): Promise<T | null>
  remove(key: string): Promise<void>
  removeAll(): Promise<void>
}
```

> 存储 key 自动添加前缀 `plugin:{pluginId}:`，插件不需要关心。

### 4.4 SSH API

复用 RD 内置连接池，**禁止插件新建独立 SSH 进程**。

```typescript
interface CommandResult {
  success: boolean
  output: string
  exitCode: number
}

interface SshApi {
  runCommand(serverId: string, cmd: string): Promise<CommandResult>
  getSession(serverId: string): Promise<SshSession | null>
}
```

### 4.5 SFTP API

```typescript
interface SftpApi {
  upload(serverId: string, localPath: string, remotePath: string): Promise<void>
  download(serverId: string, remotePath: string, localPath: string): Promise<void>
  list(serverId: string, remotePath: string): Promise<SftpFile[]>
  delete(serverId: string, remotePath: string): Promise<void>
}
```

### 4.6 服务器管理 API

```typescript
interface ServerItem {
  id: string
  name: string
  host: string
  port: number
  username: string
  authType: 'password' | 'privateKey'
  group?: string
  tags?: string[]
}

interface ServerApi {
  listAll(): Promise<ServerItem[]>
  add(item: Omit<ServerItem, 'id'>): Promise<ServerItem>
  remove(serverId: string): Promise<void>
  update(serverId: string, patch: Partial<ServerItem>): Promise<ServerItem>
}
```

### 4.7 HTTP API

带域名权限校验的网络请求。

```typescript
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE'

interface HttpApi {
  request<T>(url: string, opt: { method?: HttpMethod; body?: unknown; headers?: Record<string, string> }): Promise<T>
  get<T>(url: string, headers?: Record<string, string>): Promise<T>
  post<T>(url: string, body: unknown, headers?: Record<string, string>): Promise<T>
}
```

### 4.8 隧道 API（端口转发）

详见 [第十一章](#十一端口转发插件专项规范)。

---

## 五、事件总线规范

### 5.1 事件总线接口

```typescript
interface EventBus {
  on<T extends keyof RdEventMap>(event: T, handler: (...args: RdEventMap[T]) => void): void
  off<T extends keyof RdEventMap>(event: T, handler: (...args: RdEventMap[T]) => void): void
  offAll(owner: object): void
  emit<T extends keyof RdEventMap>(event: T, ...args: RdEventMap[T]): void
}
```

### 5.2 全量事件列表

#### 连接生命周期事件

| 事件 | 参数 | 触发时机 |
|------|------|---------|
| `connection:before-connect` | `server: ServerItem` | 发起 SSH 连接前（可拦截、修改） |
| `connection:success` | `serverId: string` | 连接建立成功 |
| `connection:error` | `serverId: string, error: Error` | 连接失败 |
| `connection:close` | `serverId: string` | 会话断开 |
| `connection:batch-start` | `batchId: string, serverIds: string[]` | 批量多服务器执行开始 |
| `connection:batch-finish` | `batchResult: BatchResult` | 批量执行全部完成 |

#### 终端事件

| 事件 | 参数 | 触发时机 |
|------|------|---------|
| `terminal:new-tab` | `terminalId: string, serverId?: string` | 新建终端标签 |
| `terminal:output` | `terminalId: string, data: Uint8Array` | 终端输出日志流 |
| `terminal:input` | `terminalId: string, input: string` | 用户输入命令 |
| `terminal:theme-change` | `theme: string` | 切换终端主题 |

#### SFTP 文件事件

| 事件 | 参数 | 触发时机 |
|------|------|---------|
| `sftp:upload-start` | `serverId: string, local: string, remote: string` | 开始上传 |
| `sftp:upload-finish` | `serverId: string, ok: boolean` | 上传完成 |
| `sftp:download-start` | `serverId: string, remote: string, local: string` | 开始下载 |
| `sftp:download-finish` | `serverId: string, ok: boolean` | 下载完成 |
| `sftp:file-delete` | `serverId: string, path: string` | 删除文件 |

#### UI 事件

| 事件 | 参数 | 触发时机 |
|------|------|---------|
| `ui:server-list-render` | - | 服务器列表渲染完成 |
| `ui:main-menu-render` | - | 主菜单渲染 |
| `ui:notification` | `msg: string` | RD 弹出系统通知 |

#### 隧道事件（端口转发）

| 事件 | 参数 | 触发时机 |
|------|------|---------|
| `tunnel:start` | `tunnelId: string` | 隧道成功启动 |
| `tunnel:stop` | `tunnelId: string` | 隧道已停止 |
| `tunnel:error` | `tunnelId: string, code: TunnelErrorCode, msg: string` | 隧道发生错误 |

#### 系统事件

| 事件 | 参数 | 触发时机 |
|------|------|---------|
| `task:cron-run` | `taskId: string` | 定时任务触发 |
| `log:write` | `level: string, msg: string` | RD 写入本地日志 |
| `config:sync` | - | 全局配置同步 |

---

## 六、权限系统规范

### 6.1 权限列表

| 权限标识符 | 说明 | 风险等级 |
|-----------|------|---------|
| `network.http` | 网络 HTTP 请求 | 中 |
| `storage.read` | 插件隔离存储读取 | 低 |
| `storage.write` | 插件隔离存储写入 | 低 |
| `server.read` | 读取服务器列表（不含密码/密钥） | 中 |
| `server.write` | 新增/修改/删除服务器配置 | 高 |
| `ssh.run` | 执行 SSH 命令 | **极高** |
| `sftp.operate` | SFTP 上传下载文件 | 高 |
| `ui.notification` | 弹窗通知 | 低 |
| `ui.dialog` | 打开对话框 | 低 |
| `tunnel.manage` | 管理 SSH 端口转发（含远程转发） | **极高** |

### 6.2 权限机制

1. **声明制**：插件在 `manifest.json` 的 `permissions` 字段声明所需权限
2. **授权制**：安装时弹窗展示所有权限，用户确认后才授予
3. **可撤回**：用户可随时在插件管理面板撤销权限
4. **拦截制**：无声明权限的 API 调用直接抛出 `PERMISSION_DENIED` 异常
5. **高危提示**：高风险权限（`ssh.run`、`server.write`、`tunnel.manage`）安装时红色标红警告

### 6.3 全局权限开关

RD 设置页提供全局总开关：
- 🔒 禁止所有插件执行 SSH 命令
- 🔒 禁止所有插件修改服务器配置
- 🔒 禁止远程端口转发（Remote -R）

---

## 七、插件管理器设计

### 7.1 核心模块

```
plugin/
├─ mod.rs          # 插件模块入口
├─ manifest.rs     # manifest.json 解析与校验
├─ manager.rs      # PluginManager 核心管理器
├─ sandbox.rs      # 权限沙箱校验
├─ hot_reload.rs   # 文件监听热重载
└─ store.rs        # 插件持久化存储（启用状态、授权）
```

### 7.2 PluginManager 职责

1. **插件扫描**：扫描 `${appData}/rd/plugins` 目录下所有插件
2. **Manifest 校验**：解析 manifest.json，校验格式、版本、依赖
3. **插件加载**：在沙箱中加载插件脚本，创建实例
4. **生命周期调度**：按顺序调用 init / enable / disable / uninstall
5. **依赖解析**：校验插件依赖关系、检测冲突
6. **权限校验**：所有 SDK API 调用前检查权限
7. **事件转发**：维护事件总线，转发事件给订阅插件
8. **热重载**：监听插件目录变化，自动重新加载

### 7.3 插件持久化存储

```rust
struct PluginStoreItem {
    pub id: String,
    pub enabled: bool,
    pub granted_permissions: Vec<String>,
    pub config: serde_json::Value,
    pub install_time: i64,
}
```

### 7.4 Tauri Commands（前端调用）

```rust
// 获取插件列表
fn plugin_list() -> Vec<PluginInfo>

// 启用/禁用插件
fn plugin_toggle(id: String, enabled: bool) -> Result<()>

// 卸载插件
fn plugin_uninstall(id: String) -> Result<()>

// 本地安装插件
fn plugin_install_from_file(path: String) -> Result<PluginInfo>

// 获取插件配置
fn plugin_get_config(id: String) -> Result<Value>

// 设置插件配置
fn plugin_set_config(id: String, config: Value) -> Result<()>

// 获取已授权权限
fn plugin_get_granted_permissions(id: String) -> Result<Vec<String>>

// 设置权限
fn plugin_set_granted_permissions(id: String, perms: Vec<String>) -> Result<()>

// 热重载插件
fn plugin_reload(id: String) -> Result<()>
```

---

## 八、插件UI界面设计

### 8.1 设计原则

1. **不破坏原有布局**：所有注入元素可选，禁用插件即彻底消失
2. **统一设计语言**：和 RD 现有组件、主题、暗黑/浅色模式保持一致
3. **SDK 受控**：插件只能通过 SDK API 注册 UI，不允许直接操作 DOM
4. **安全提示**：权限敏感操作增加提示，异常插件友好展示错误

### 8.2 插件管理主页面（Settings → Plugins）

RD 设置页新增「插件」Tab，与「通用、终端、服务器、快捷键」平级。

页面分为 3 个子 Tab：

#### Tab1：已安装插件

布局：左侧插件列表，右侧详情面板。

**左侧列表**
- 每行：插件图标 + 名称 + 版本 + 描述 + 状态标签 + 开关
- 状态标签：`已启用` / `已禁用` / `加载失败（红色）`
- 操作按钮：`配置`｜`权限`｜`卸载`

**右侧详情面板**
- 头部：图标、名称、版本、作者、简介
- 元信息：插件 ID、API 版本、最低 RD 版本、分类
- 已授予权限列表：每个权限展示名称+说明，可撤销；高危权限标红
- 互斥提示：冲突插件红色警告条
- 操作按钮：【打开配置】【管理权限】【卸载】

#### Tab2：本地安装

- 拖拽区域：大虚线框，提示「拖拽 .rdplugin 文件到此进行安装」
- 按钮：【选择 rdplugin 文件】

**安装流程弹窗**
1. 展示插件基础信息：图标、名称、版本、作者
2. **权限清单（重点安全）**：列出全部申请权限，高危标红
3. 风险提示：「该插件为第三方插件，请仔细审阅权限，存在安全风险」
4. 按钮：【取消】｜【确认并授予权限】

#### Tab3：调试控制台

面向插件开发者：
- 日志输出区：插件 `ctx.log` 日志，带插件 ID、时间戳、级别颜色
- 操作按钮：全部热重载、清空日志
- 插件筛选：可选择单个插件查看日志

### 8.3 插件配置弹窗

根据 `config.schema.json` 自动渲染表单。

弹窗结构：
1. 标题：`插件配置 - {插件名称}`
2. 表单区域：自动渲染组件
3. 底部按钮：【重置默认】｜【取消】｜【保存】

保存后：
- 持久化存储配置
- 调用插件 `onConfigChange(newConfig)`
- toast 提示配置已保存

### 8.4 插件注入的动态UI

插件通过 SDK API 注册，内核统一渲染，插件禁用时自动移除。

| UI 类型 | SDK API | 说明 |
|---------|---------|------|
| 顶部工具栏按钮 | `registerTopBarButton` | 和原生按钮并排 |
| 设置页菜单项 | `registerSettingItem` | 设置页增加跳转入口 |
| 通知 Toast | `ui.notify` | 右下角弹出，统一样式 |
| 确认对话框 | `ui.confirm` | 系统统一确认框 |
| 输入对话框 | `ui.prompt` | 系统统一输入框 |

> **v1 约束**：禁止插件传入自定义 HTML / Vue 组件 / CSS，避免 XSS 风险。v2 可考虑 iframe 沙箱隔离。

### 8.5 异常状态界面

| 异常场景 | 界面表现 |
|---------|---------|
| 插件加载失败 | 列表行红色「加载失败」标签，tooltip 显示错误详情，开关置灰 |
| 运行时异常 | 调试控制台打印堆栈，toast 警告「插件 xxx 发生运行异常，已自动禁用」 |
| 权限被撤销 | 拦截 API 调用，toast 提示「插件 xxx 权限已被撤销，部分功能不可用」 |
| 互斥冲突 | 详情面板顶部红色警告条：「该插件与 xxx 插件互斥，请禁用其中一个」 |

---

## 九、安全沙箱设计

### 9.1 沙箱选型

推荐使用 `deno_core` 作为插件脚本执行沙箱，在 Rust 侧嵌入 V8 隔离环境。

### 9.2 安全约束

#### 1. 权限白名单
- 所有系统能力必须通过 SDK API 暴露
- 插件不能直接访问 Node.js / Rust 原生 API
- 无声明权限的 API 调用直接拦截

#### 2. 文件访问隔离
- 插件仅允许读写自身存储目录
- 禁止访问系统敏感目录（`/etc`、用户 `.ssh` 私钥目录等）
- 文件路径自动前缀隔离

#### 3. 进程调用拦截
- 插件禁止直接调用本地 shell / exec
- 执行命令必须通过 `ctx.ssh.runCommand`（远程 SSH）
- 本地命令执行需单独权限 + 二次确认

#### 4. 网络限制
- HTTP 请求仅允许公网常规 API
- 禁止内网 IP 扫描（可配置白名单）
- 域名级别的访问控制

#### 5. 插件签名校验
- 官方商店插件带开发者数字签名
- 本地导入未签名插件增加风险提示弹窗

#### 6. 资源限制
- 单插件 CPU / 内存占用上限
- 卡死插件自动隔离禁用
- 防止插件死循环阻塞主程序

### 9.3 异常隔离

- 插件所有异常自动捕获，不影响主程序
- 插件崩溃自动禁用，弹窗提示用户
- 插件 disable 时确保所有资源释放（事件监听、定时器、UI 注册）

---

## 十、热加载机制

### 10.1 热加载流程

```
插件目录文件变更（新增/修改 manifest、入口文件）
        ↓
文件监听器检测到变化
        ↓
调用旧插件实例 disable() 生命周期
        ↓
注销全部事件监听与 UI 注册
        ↓
销毁旧沙箱实例
        ↓
重新解析 manifest，校验版本与权限
        ↓
创建新沙箱，加载插件脚本
        ↓
调用新实例 init()
        ↓
若插件状态为启用 → 调用 enable()
        ↓
热加载完成，毫秒级生效
```

### 10.2 文件监听

- 监听目录：`${appData}/rd/plugins/`
- 防抖：500ms 防抖，避免频繁触发
- 触发条件：manifest.json 变更、入口文件变更、资源文件变更

### 10.3 注意事项

1. 插件必须正确实现 `disable()`，释放所有资源，否则内存泄漏
2. 热加载失败时保留旧实例，不影响运行
3. `hotReload: false` 的插件不支持热加载，需重启生效

---

## 十一、端口转发插件专项规范

> 规范标识：`RD-PortForward-Spec v1.0`
> 依赖：plugin-sdk v1
> 所需权限：`tunnel.manage`（高危）

### 11.1 设计原则

- **底层内核实现**：TCP 监听、SSH 隧道逻辑永远在 Rust 内核实现
- **插件只做管理层**：规则 CRUD、UI、自动启停、事件联动
- **会话绑定**：隧道生命周期强绑定 SSH 会话，会话销毁隧道强制关闭
- **禁止后台残留**：不允许脱离 SSH 会话的独立端口监听

### 11.2 数据模型

#### TunnelMode 转发模式

```typescript
type TunnelMode = "local" | "remote" | "dynamic"
```

| 值 | SSH 参数 | 说明 |
|----|---------|------|
| `local` | `-L` | 本地转发：本机端口 → 服务器侧目标地址 |
| `remote` | `-R` | 远程转发：服务器端口 → 本机侧目标地址（高危） |
| `dynamic` | `-D` | 动态 SOCKS5 代理 |

#### TunnelRule（转发规则，持久化）

```typescript
interface TunnelRule {
  id: string                    // uuid v4，全局唯一
  serverId: string              // 绑定服务器ID，隧道依附该SSH会话
  mode: TunnelMode              // 转发模式
  localAddr: string             // 本地监听地址，如 127.0.0.1 / 0.0.0.0
  localPort: number             // 本地监听端口 1-65535
  remoteAddr?: string           // remote/local 模式有效
  remotePort?: number           // remote/local 模式有效
  autoStart: boolean            // 服务器连接成功自动启动隧道
  tags?: string[]               // 自定义标签，可选
  comment?: string              // 用户备注，可选
}
```

#### TunnelStatus（运行时状态）

```typescript
interface TunnelStatus {
  tunnelId: string
  running: boolean
  error?: string                // 启动失败时错误信息
  sessionId?: string            // 当前绑定的 ssh 会话 id
}
```

### 11.3 校验约束（内核强制）

1. `localPort` 范围：`1-65535`
2. `localAddr` 必须是合法 IP 地址
3. `local/remote` 模式必须填写 `remoteAddr`、`remotePort`
4. `dynamic` 模式禁止携带 `remoteAddr / remotePort`
5. `serverId` 必须存在于 RD 服务器列表
6. `remote` 模式受全局开关控制，可全局禁用

### 11.4 Tunnel API

```typescript
interface TunnelApi {
  // 创建转发规则（持久化，不启动隧道）
  createRule(rule: Omit<TunnelRule, "id">): Promise<TunnelRule>

  // 删除规则，若正在运行先停止
  removeRule(tunnelId: string): Promise<void>

  // 查询规则列表，可按 serverId 过滤
  listRules(serverId?: string): Promise<TunnelRule[]>

  // 启动隧道（前置：对应SSH会话必须已连接）
  startTunnel(tunnelId: string): Promise<TunnelStatus>

  // 停止隧道
  stopTunnel(tunnelId: string): Promise<void>

  // 获取全部隧道实时运行状态
  listStatus(): Promise<TunnelStatus[]>
}
```

### 11.5 错误码

```typescript
enum TunnelErrorCode {
  RULE_NOT_FOUND = "RULE_NOT_FOUND",           // 找不到规则
  SESSION_NOT_AVAILABLE = "SESSION_NOT_AVAILABLE", // 服务器未建立SSH会话
  PORT_IN_USE = "PORT_IN_USE",                 // 本地端口被占用
  PORT_INVALID = "PORT_INVALID",               // 端口不合法
  REMOTE_FORBIDDEN = "REMOTE_FORBIDDEN",       // remote模式被全局禁用
  PERMISSION_DENIED = "PERMISSION_DENIED",     // 权限不足
  SSH_TUNNEL_ERROR = "SSH_TUNNEL_ERROR"        // SSH隧道建立失败
}
```

### 11.6 隧道事件

```typescript
'tunnel:start': [tunnelId: string]            // 隧道成功启动
'tunnel:stop': [tunnelId: string]             // 隧道已停止
'tunnel:error': [tunnelId: string, code: TunnelErrorCode, msg: string] // 隧道错误
```

### 11.7 SSH 会话联动

1. `connection:success(serverId)` → 插件遍历该服务器 `autoStart=true` 的规则，自动启动
2. `connection:close(serverId)` → **内核自动停止该会话全部隧道**，同时 emit `tunnel:stop`
3. 插件只做 UI 提示，不需要手动 stop

> 重要：会话断开停止隧道是内核行为，不是插件责任，防止插件 bug 导致端口残留。

### 11.8 导入导出格式

文件后缀：`.rd-tunnels.json`

```json
{
  "$schema": "https://rd.dev/spec/portforward/v1/schema.json",
  "specVersion": "1.0",
  "exportTime": "2026-08-15T00:00:00Z",
  "rules": [
    {
      "id": "uuid-xxx",
      "serverId": "server-uuid",
      "mode": "local",
      "localAddr": "127.0.0.1",
      "localPort": 33060,
      "remoteAddr": "127.0.0.1",
      "remotePort": 3306,
      "autoStart": true,
      "comment": "MySQL转发"
    }
  ]
}
```

### 11.9 插件 manifest 约定

做端口转发的插件，manifest 必须遵守：

```json
{
  "id": "port-forward-manager",
  "name": "端口转发管理器",
  "version": "1.0.0",
  "apiVersion": "v1",
  "category": "connection",
  "permissions": ["tunnel.manage", "storage.read", "storage.write"],
  "minRdVersion": "1.6.0",
  "hotReload": true
}
```

### 11.10 插件 UI 交互约定

1. **入口**：建议注册顶部工具栏按钮，id 约定：`port-forward-open`
2. **配置弹窗表格列**：状态 | 模式 | 绑定服务器 | 本地地址:端口 | 远程地址:端口 | 自动启动 | 操作
3. **新增表单字段顺序**：绑定服务器 → 转发模式 → 本地监听地址 → 本地端口 → 远程地址/端口 → 自动启动 → 备注
4. **高危提示**：
   - 选择 `remote` 模式：红色提示「远程转发会将服务器端口暴露至本地网络，请谨慎使用」
   - `localAddr` 填 `0.0.0.0`：提示「监听全部网卡，其他设备可访问此端口」
5. **导入导出**：支持 `.rd-tunnels.json` 格式

### 11.11 安全约束

1. 隧道生命周期必须绑定 SSH 会话，会话关闭内核强制关闭隧道
2. `remote` 模式属于高危，必须有明确 UI 警告；内核支持全局禁用
3. 监听 `0.0.0.0` 需要二次确认弹窗
4. 插件卸载时，全部隧道必须停止
5. 不允许插件绕过 TunnelApi 直接操作 TCP 端口

---

## 十二、落地迭代路线图

### Phase 1：基础骨架（v0.1）

- [ ] plugin-sdk 包（类型定义）
- [ ] manifest 解析与校验
- [ ] PluginManager 基础扫描加载
- [ ] 插件持久化存储（启用状态、权限）
- [ ] 生命周期：init / enable / disable
- [ ] 前端插件管理页面基础 UI（已安装列表）

### Phase 2：事件总线 + 基础 SDK（v0.2）

- [ ] EventBus 实现
- [ ] storage API
- [ ] ui.notify / ui.dialog API
- [ ] log API
- [ ] 前端插件配置弹窗（schema 自动渲染）
- [ ] 权限校验基础框架

### Phase 3：业务 SDK API（v0.3）

- [ ] ssh.runCommand API
- [ ] sftp API
- [ ] server API
- [ ] http API
- [ ] sandbox 权限校验拦截
- [ ] 高危权限 UI 警告

### Phase 4：热重载 + 安装（v0.4）

- [ ] 文件监听热加载
- [ ] .rdplugin 打包格式
- [ ] 本地拖拽安装
- [ ] 安装流程弹窗（权限确认）
- [ ] 插件调试控制台

### Phase 5：端口转发专项（v0.5）

- [ ] Rust 侧 tunnel 底层实现（L/R/D 三种）
- [ ] TunnelApi SDK
- [ ] 隧道事件
- [ ] 端口转发插件（官方）
- [ ] 导入导出 .rd-tunnels.json

### Phase 6：生态完善（v1.0）

- [ ] 插件市场（在线浏览、下载、更新）
- [ ] 插件签名校验
- [ ] 官方插件审核机制
- [ ] 插件开发者文档站
- [ ] 插件模板脚手架

---

## 十三、附录：完整 SDK 类型定义

```typescript
/**
 * RD Plugin SDK v1
 * 插件SDK，插件开发者只依赖此包，禁止导入RD内部业务模块
 */

// ===================== Manifest 插件元数据 =====================
export interface PluginManifest {
  id: string
  name: string
  version: string
  apiVersion: 'v1'
  author: string
  description: string
  category: 'connection' | 'command' | 'file' | 'terminal' | 'monitor' | 'notify' | 'other'
  entry: string
  configSchema?: string
  icon?: string
  permissions: PluginPermission[]
  conflict?: string[]
  requires?: string[]
  minRdVersion: string
  hotReload: boolean
}

export type PluginPermission =
  | 'network.http'
  | 'storage.read'
  | 'storage.write'
  | 'server.read'
  | 'server.write'
  | 'ssh.run'
  | 'sftp.operate'
  | 'ui.notification'
  | 'ui.dialog'
  | 'tunnel.manage'

// ===================== 事件总线类型 =====================
export type RdEventMap = {
  // 连接生命周期
  'connection:before-connect': [server: ServerItem]
  'connection:success': [serverId: string]
  'connection:error': [serverId: string, error: Error]
  'connection:close': [serverId: string]
  'connection:batch-start': [batchId: string, serverIds: string[]]
  'connection:batch-finish': [batchResult: BatchResult]

  // 终端
  'terminal:new-tab': [terminalId: string, serverId?: string]
  'terminal:output': [terminalId: string, data: Uint8Array]
  'terminal:input': [terminalId: string, input: string]
  'terminal:theme-change': [theme: string]

  // SFTP文件
  'sftp:upload-start': [serverId: string, local: string, remote: string]
  'sftp:upload-finish': [serverId: string, ok: boolean]
  'sftp:download-start': [serverId: string, remote: string, local: string]
  'sftp:download-finish': [serverId: string, ok: boolean]
  'sftp:file-delete': [serverId: string, path: string]

  // UI
  'ui:server-list-render': []
  'ui:main-menu-render': []
  'ui:notification': [msg: string]

  // 隧道
  'tunnel:start': [tunnelId: string]
  'tunnel:stop': [tunnelId: string]
  'tunnel:error': [tunnelId: string, code: TunnelErrorCode, msg: string]

  // 系统
  'task:cron-run': [taskId: string]
  'log:write': [level: string, msg: string]
  'config:sync': []
}

export interface EventBus {
  on<T extends keyof RdEventMap>(event: T, handler: (...args: RdEventMap[T]) => void): void
  off<T extends keyof RdEventMap>(event: T, handler: (...args: RdEventMap[T]) => void): void
  offAll(owner: object): void
  emit<T extends keyof RdEventMap>(event: T, ...args: RdEventMap[T]): void
}

// ===================== 业务实体类型 =====================
export interface ServerItem {
  id: string
  name: string
  host: string
  port: number
  username: string
  authType: 'password' | 'privateKey'
  group?: string
  tags?: string[]
}

export interface CommandResult {
  success: boolean
  output: string
  exitCode: number
}

export interface BatchResult {
  batchId: string
  total: number
  success: number
  fail: number
  results: Record<string, CommandResult>
}

// ===================== 隧道相关 =====================
export type TunnelMode = 'local' | 'remote' | 'dynamic'

export interface TunnelRule {
  id: string
  serverId: string
  mode: TunnelMode
  localAddr: string
  localPort: number
  remoteAddr?: string
  remotePort?: number
  autoStart: boolean
  tags?: string[]
  comment?: string
}

export interface TunnelStatus {
  tunnelId: string
  running: boolean
  error?: string
  sessionId?: string
}

export enum TunnelErrorCode {
  RULE_NOT_FOUND = 'RULE_NOT_FOUND',
  SESSION_NOT_AVAILABLE = 'SESSION_NOT_AVAILABLE',
  PORT_IN_USE = 'PORT_IN_USE',
  PORT_INVALID = 'PORT_INVALID',
  REMOTE_FORBIDDEN = 'REMOTE_FORBIDDEN',
  PERMISSION_DENIED = 'PERMISSION_DENIED',
  SSH_TUNNEL_ERROR = 'SSH_TUNNEL_ERROR'
}

// ===================== UI注册相关 =====================
export interface TopBarButtonOption {
  id: string
  text: string
  icon?: string
  onClick: () => void
}

export interface SettingItemOption {
  group: string
  name: string
  onClick: () => void
}

export interface UiApi {
  registerTopBarButton(opt: TopBarButtonOption): void
  removeTopBarButton(id: string): void

  registerSettingItem(opt: SettingItemOption): void
  removeSettingItem(id: string): void

  openPluginConfig(pluginId: string): void

  notify(title: string, msg: string, type: 'success' | 'warn' | 'error' | 'info'): void

  confirm(message: string): Promise<boolean>
  prompt(message: string, defaultValue?: string): Promise<string | null>
}

// ===================== 存储API =====================
export interface PluginStorage {
  set(key: string, value: unknown): Promise<void>
  get<T = unknown>(key: string): Promise<T | null>
  remove(key: string): Promise<void>
  removeAll(): Promise<void>
}

// ===================== SSH / SFTP API =====================
export interface SshApi {
  runCommand(serverId: string, cmd: string): Promise<CommandResult>
  getSession(serverId: string): Promise<unknown | null>
}

export interface SftpApi {
  upload(serverId: string, localPath: string, remotePath: string): Promise<void>
  download(serverId: string, remotePath: string, localPath: string): Promise<void>
  list(serverId: string, remotePath: string): Promise<unknown[]>
  delete(serverId: string, remotePath: string): Promise<void>
}

// ===================== Server API =====================
export interface ServerApi {
  listAll(): Promise<ServerItem[]>
  add(item: Omit<ServerItem, 'id'>): Promise<ServerItem>
  remove(serverId: string): Promise<void>
  update(serverId: string, patch: Partial<ServerItem>): Promise<ServerItem>
}

// ===================== HTTP API =====================
export type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE'

export interface HttpApi {
  request<T>(url: string, opt: { method?: HttpMethod; body?: unknown; headers?: Record<string, string> }): Promise<T>
  get<T>(url: string, headers?: Record<string, string>): Promise<T>
  post<T>(url: string, body: unknown, headers?: Record<string, string>): Promise<T>
}

// ===================== 隧道 API =====================
export interface TunnelApi {
  createRule(rule: Omit<TunnelRule, 'id'>): Promise<TunnelRule>
  removeRule(tunnelId: string): Promise<void>
  listRules(serverId?: string): Promise<TunnelRule[]>
  startTunnel(tunnelId: string): Promise<TunnelStatus>
  stopTunnel(tunnelId: string): Promise<void>
  listStatus(): Promise<TunnelStatus[]>
}

// ===================== 插件上下文Context =====================
export interface RDContext {
  readonly pluginId: string
  readonly manifest: PluginManifest

  ui: UiApi
  storage: PluginStorage
  ssh: SshApi
  sftp: SftpApi
  server: ServerApi
  http: HttpApi
  tunnel: TunnelApi

  log: {
    info(msg: string): void
    warn(msg: string): void
    error(msg: string): void
  }
}

// ===================== BasePlugin 基类 =====================
export abstract class BasePlugin {
  public manifest!: PluginManifest

  abstract init(ctx: RDContext, bus: EventBus): Promise<void>
  abstract enable(): Promise<void>
  abstract disable(): Promise<void>
  abstract uninstall(): Promise<void>
  abstract onConfigChange(newConfig: Record<string, unknown>): Promise<void>
}

export type PluginClass = new () => BasePlugin
```

---

**文档结束**

> 本文档为 RD 插件系统 v1.0 完整设计规范，可直接作为开发依据。后续迭代请更新版本号并维护变更记录。
