# RD 远程文件管理器（专业双栏版）产品需求文档 V2.1

**更新说明**：本次V2.1版本新增完整插件化扩展体系，包含插件运行时架构、插件中心市场、能力边界规范、沙箱安全机制与开发者接入标准，支持第三方生态拓展；同步优化原有功能表述，补全插件与核心模块的交互规则，全文档逻辑闭环，可直接用于开发落地。

**开发技术栈**：Tauri2.0 + Rust(russh) + React + Xterm.js
**适配平台**：Windows、macOS、Linux 全平台
**产品定位**：RD 是一款高信息密度专业级Linux远程管理客户端，基于单SSH连接复用SFTP文件管理与终端能力，采用双栏本地-远程对照操作，配套完整服务器信息、传输队列与快捷工具；同时开放标准化插件生态，支持按需扩展功能，兼顾操作效率、运维专业性与场景可定制性。

## 一、整体布局框架
界面采用经典五区固定式布局，所有核心功能一屏可达，无需多级跳转：
1.  **顶部全局标签栏**：多服务器标签页管理、全局快捷入口
2.  **左侧导航边栏**：连接管理、主机分组、工具菜单、连接信息概览
3.  **中间主工作区**：左右双栏布局，左侧本地文件、右侧远程文件，支持对照浏览与拖拽传输；工具切换后可承载插件中心、终端全屏等其他功能页
4.  **右侧功能面板**：服务器信息、快捷操作、传输队列三大区块
5.  **底部终端区**：内置SSH终端，支持多终端标签
6.  **底部状态栏**：全局状态、传输速率、网络延迟展示

## 二、顶部全局标签栏
### 2.1 栏位元素
- 左上角：软件品牌标识「RD」
- 中间标签区：多服务器标签页，支持横向滚动
  - 每个标签显示：自定义服务器名称、用户名@IP地址、连接状态指示灯（绿色=已连接，灰色=未连接，红色=异常）
  - 标签右侧带关闭按钮，标签栏末尾带「+」按钮用于新建连接/新标签
- 右上角功能按钮：快速连接按钮、窗口布局切换按钮、应用设置入口图标

### 2.2 标签交互规则
- 点击标签切换对应服务器会话，主工作区、右侧面板、终端同步切换
- 支持标签拖拽排序
- 标签右键菜单：关闭当前标签、关闭其他标签、关闭右侧标签、复制连接信息、新建同主机标签

## 三、左侧导航边栏
边栏固定宽度，自上而下分为四大模块：连接管理、我的连接、分组、工具，底部为连接信息概览。

### 3.1 连接管理
- 顶部标题「连接管理」，右侧带「+」新建连接按钮
- 搜索输入框：支持按主机名称、IP搜索，占位提示「搜索主机或IP (⌘K)」，支持快捷键 `Ctrl+K` / `Cmd+K` 快速聚焦搜索框

### 3.2 我的连接
- 列表展示所有已保存主机，按最近连接排序
- 单条主机项包含：连接状态圆点、主机自定义名称、用户名@IP地址
- 点击主机项建立连接并切换当前会话；右键弹出操作菜单：编辑、删除、复制地址、在新标签打开
- 示例主机：prod-server、staging、dev-server、test-server、backup

### 3.3 分组
- 支持创建自定义分组，分组名称后带主机数量角标
- 示例分组：Web 服务器(3)、数据库(2)、测试环境(2)
- 分组支持折叠/展开，支持主机拖拽移入分组，支持分组内排序
- 分组右键菜单：重命名、删除分组、新建主机

### 3.4 工具菜单
垂直排列功能入口，当前选中项高亮，包含：
1.  **SFTP**（默认选中）：进入双栏文件管理界面
2.  **SSH 终端**：全屏展示终端界面
3.  **端口转发**：端口转发规则配置管理
4.  **远程命令**：批量/预设远程命令管理
5.  **密钥管理**：SSH公私钥生成、导入、管理
6.  **插件中心**：进入插件市场与已安装插件管理页面

### 3.5 连接信息概览
- 位于边栏底部，展示当前选中主机的详细连接参数
- 展示信息：主机名、日连接时长、SSH版本、加密算法、压缩状态
- 底部「会话管理」按钮，点击展开会话详情、断开连接等操作

## 四、中间主工作区
### 4.1 SFTP双栏文件管理器（默认页）
核心工作区采用左右等分双栏布局，左为本地文件，右为远程文件，支持拖拽互传。

#### 4.1.1 公共头部
- 区域标题：**SFTP 文件管理器**

#### 4.1.2 本地文件栏（左栏）
- **顶部工具栏**：路径面包屑显示当前本地路径，支持点击各级目录跳转；操作按钮组包含前进、后退、刷新、视图切换、目录内搜索；路径支持点击切换为输入模式，手动输入本地路径跳转
- **文件列表**：列表表头为名称、类型、修改时间；行内包含文件类型图标、文件名、文件类型标识、修改日期时间；区分文件夹与各类文件图标样式；支持单选、框选、Ctrl多选、Shift连选
- **底部统计栏**：显示当前目录文件夹数量、文件数量、总大小，示例：`6 个文件夹, 7 个文件 | 526.9 KB`

#### 4.1.3 远程文件栏（右栏）
- **顶部工具栏**：标注当前服务器名称，示例「远程: prod-server」；路径面包屑显示当前远程路径，支持点击各级跳转；操作按钮组与本地栏对齐
- **文件列表**：列表表头为名称、大小、类型、修改时间、权限、所有者；行内包含文件类型图标、文件名、文件大小、类型、修改日期、权限位、所有者；文件夹置顶排序，支持按任意表头升序/降序排列
- **底部统计栏**：显示当前远程目录统计信息，右侧带「SFTP」标识与安全锁图标，标识当前传输协议与加密状态

#### 4.1.4 双栏核心交互
- **拖拽传输**：本地文件拖拽至远程栏即上传，远程文件拖拽至本地栏即下载
- **双向同步**：远程目录切换时，自动同步底部终端工作目录
- **右键菜单**：本地/远程文件统一支持打开、重命名、删除、复制路径、属性查看；远程文件额外支持修改权限、在终端打开、添加到收藏；支持插件扩展右键菜单项

### 4.2 插件中心页面
点击左侧「插件中心」后，主工作区切换为插件管理界面，分为两个标签页：

#### 4.2.1 插件市场
- 顶部：插件搜索框、分类筛选（全部、官方、工具类、协议类、主题类、运维类）、排序方式（推荐、最新、下载量、评分）
- 主体：插件卡片网格布局，每张卡片包含插件图标、名称、开发者、简介、下载量、评分、「安装」按钮
- 点击插件卡片进入详情页，展示完整功能介绍、版本信息、权限声明、更新日志、用户评价

#### 4.2.2 我的插件
- 已安装插件列表，展示插件名称、版本号、启用状态、安装时间
- 操作项：启用/禁用、更新、卸载、查看权限
- 支持批量启用/禁用、批量更新

## 五、右侧功能面板
固定宽度侧边面板，自上而下分为三个区块：服务器信息、快捷操作、传输队列。

### 5.1 服务器信息
- 区块标题：服务器信息
- 信息项：主机、用户、协议、端口、系统、在线时长、连接状态
- 连接状态：绿色文字+圆点标识「已连接」
- 操作按钮：红色「断开连接」主按钮

### 5.2 快捷操作
- 区块标题：快捷操作
- 2×3 网格图标按钮，共6个官方默认功能入口：打开终端、新建文本、上传文件、同步目录、权限设置、查找文件
- 支持已安装插件向快捷操作区注册按钮，插件按钮统一放在官方按钮下方

### 5.3 传输队列
- 区块标题：传输队列
- 顶部标签切换：上传、下载，标签后带任务数量角标
- 任务列表：每条任务显示文件名、文件大小、进度条、传输速度、操作按钮（暂停/取消）
- 底部「负载信息」折叠项，展示当前传输负载与系统资源占用

## 六、底部终端区
### 6.1 终端头部
- 终端标签栏：默认一个终端标签，显示「终端 + 服务器名」，支持「+」新建多个终端标签
- 右上角操作按钮：终端面板全屏、面板高度调节、更多操作

### 6.2 终端主体
- 基于 Xterm.js 实现完整交互式 SSH 终端
- 显示登录信息、上次登录时间、命令执行结果
- 命令提示符格式：`用户名@主机名:当前路径#`
- 支持彩色输出、代码高亮、鼠标选中复制、右键粘贴
- 终端工作目录与右侧远程文件栏实时双向同步
- 支持插件向终端注册辅助命令、扩展终端交互能力

### 6.3 终端交互规则
- 终端面板支持拖拽上下边缘调整高度
- 支持快捷键快速唤起/收起终端面板
- 多个终端标签共享同一条 SSH 连接的不同 PTY 通道，不重复建立连接

## 七、底部全局状态栏
- 左侧：应用全局状态，示例「就绪」
- 右侧从左到右：
  - 实时上传速率，示例 `↑ 上传: 1.2 MB/s`
  - 实时下载速率，示例 `↓ 下载: 2.4 MB/s`
  - 网络延迟，示例 `● 延迟: 23ms`，延迟异常时变色提示
  - 插件运行状态：异常插件数量提示，点击可快速跳转插件管理

## 八、核心功能规则
### 8.1 SSH连接核心规则
- 单台服务器仅维持**一条SSH长连接**，同时承载SFTP文件通道、多个PTY终端通道，所有通道会话互通
- SFTP通道连接建立后常驻，终端通道按需创建，关闭终端不中断文件传输
- 支持断线自动重连，采用指数退避策略，重连后自动恢复目录、终端与传输队列状态
- 插件需通过官方API调用连接能力，禁止直接操作底层SSH连接

### 8.2 目录记忆规则
- 每台主机、每个标签独立记忆上次浏览的远程目录
- 连接建立后自动跳转至上次目录，可在主机配置中单独关闭，默认开启
- 记忆路径不存在时自动降级到用户家目录，并给出轻量提示

### 8.3 文件编辑规则
- 双击远程文件自动下载至本地临时目录，调用本地默认编辑器打开
- 监听文件保存事件，自动回传覆盖远程文件
- 上传前校验远程文件 mtime，发生变更时弹窗提示冲突，禁止强制覆盖
- 支持配置退出软件自动清理临时缓存文件
- 插件可注册自定义文件类型的默认打开方式

### 8.4 传输队列规则
- 所有上传、下载任务统一进入传输队列管理
- 支持并发数配置，默认同时传输3个文件，其余排队
- 支持单个任务暂停、取消，取消后自动清理不完整文件
- 传输中断后不自动续传，需用户手动重新发起
- 插件可通过API提交传输任务，复用核心传输能力

## 九、插件系统
### 9.1 系统定位与设计原则
- **定位**：作为核心功能的补充扩展，不侵入RD核心代码，通过标准化API开放能力，支持第三方开发者拓展场景化功能，满足不同用户的个性化运维需求
- **设计原则**：
  - 核心稳定优先：插件运行在独立沙箱，插件崩溃不影响主程序运行
  - 权限可控：所有能力均需用户授权，禁止静默获取敏感数据
  - 开箱即用：内置插件中心，一键安装卸载，无需复杂配置
  - 生态开放：提供完整开发文档与SDK，降低第三方接入门槛

### 9.2 插件技术架构
整体采用「核心层-沙箱层-插件层」三级架构，实现能力开放与安全隔离：
1.  **核心能力层**：RD原生核心模块，提供SSH连接、SFTP文件操作、终端、本地存储等基础能力
2.  **插件运行时沙箱**：独立的运行环境，封装标准化开放API，负责插件加载、权限校验、生命周期管理，隔离插件与核心系统资源
3.  **插件应用层**：第三方开发者开发的功能插件，仅能通过官方API与核心交互，无法直接访问底层接口与系统资源

### 9.3 插件分类与能力边界
#### 9.3.1 官方支持的插件类型
- **工具增强类**：扩展文件操作、右键菜单、快捷操作，例如文件对比、批量重命名、目录同步增强
- **协议扩展类**：新增文件传输协议支持，例如FTP、WebDAV、S3对象存储
- **运维工具类**：面向运维场景的辅助工具，例如日志分析、性能监控、批量脚本执行
- **主题美化类**：自定义界面主题、图标、终端配色方案
- **集成对接类**：与第三方服务对接，例如Git仓库、CI/CD平台、对象存储服务

#### 9.3.2 开放能力清单
插件可通过官方API实现以下能力：
- 读取当前主机的文件列表、文件属性、路径信息
- 调用SFTP能力执行文件上传、下载、删除、重命名操作
- 向终端发送命令、获取终端输出、注册终端辅助指令
- 扩展文件右键菜单项、工具栏按钮、快捷操作入口
- 在主工作区开辟独立面板，渲染自定义UI
- 读取当前连接状态、主机基础信息（不含敏感凭据）
- 使用本地持久化存储，保存插件自身配置数据

#### 9.3.3 禁止能力红线
- 禁止直接访问底层SSH连接句柄、修改核心连接逻辑
- 禁止读取、篡改主机密码、私钥等敏感凭据
- 禁止未经用户授权访问本地系统任意文件，仅可操作授权目录
- 禁止后台静默联网、上传用户数据
- 禁止修改RD核心程序文件、篡改其他插件运行逻辑
- 禁止劫持系统原生终端、修改系统级配置

### 9.4 插件生命周期管理
1.  **安装**：用户在插件中心点击安装，后台下载插件包，校验签名与权限，用户确认授权后完成安装
2.  **启用/禁用**：已安装插件可随时启用或禁用，禁用后插件停止运行，不占用系统资源
3.  **更新**：支持手动更新与自动更新，更新时重新校验权限，权限变更需用户二次确认
4.  **卸载**：一键卸载插件，同步清理插件本地缓存与配置数据，无残留文件
5.  **异常处理**：插件运行崩溃时，沙箱自动终止插件进程，弹出轻量提示，不影响主程序与其他功能

### 9.5 插件安全机制
- **沙箱隔离**：所有插件运行在独立沙箱环境，内存、文件、网络均隔离，崩溃不牵连主程序
- **权限申请制**：插件所需能力必须在清单中声明，安装时向用户展示，用户同意后方可生效
- **签名校验**：所有上架插件必须经过官方签名校验，防止篡改、恶意植入
- **审核机制**：第三方插件上架前需经过功能、安全双重审核，不符合规范的插件不予上架
- **数据隔离**：每个插件仅能访问自身的本地存储目录，无法读取其他插件与核心程序的敏感数据
- **网络管控**：插件联网行为可被用户管控，支持禁止单个插件访问网络

### 9.6 开发者支持
- 提供完整的插件开发SDK与API文档
- 提供官方插件示例工程与开发脚手架
- 开放插件提交与上架渠道，建立开发者反馈通道

## 十、系统设置与全局配置
### 10.1 通用设置
- 目录记忆全局开关、新建标签默认路径
- 断线自动重连开关、重连次数配置
- 退出自动清理临时文件开关

### 10.2 传输设置
- 同名文件默认处理方式：询问、覆盖、跳过、自动重命名
- 最大并发传输数
- 下载默认保存路径

### 10.3 外观设置
- 深色/浅色主题切换，默认深色专业风格
- 终端字体、字号、行高配置
- 侧边栏、文件列表显示密度调节
- 自定义主题插件启用管理

### 10.4 插件设置
- 允许第三方插件安装开关：默认开启，关闭后仅可安装官方插件
- 插件自动更新开关：默认关闭，开启后已安装插件自动更新
- 全局插件权限管理：统一查看、修改所有插件的授权状态
- 插件沙箱安全等级：标准/严格两档，严格模式下禁止插件联网
- 一键清理所有插件数据

### 10.5 安全与日志
- SSH服务器指纹校验策略
- 日志等级配置、一键导出脱敏日志
- 一键清除本地缓存与所有凭据

## 十一、非功能性需求
1.  **性能**：千级文件目录虚拟滚动无卡顿；常规目录切换响应 ≤ 300ms；终端输入无延迟感；单插件内存占用≤50MB，插件启动≤500ms
2.  **安全**：密码、私钥存入系统密钥链加密存储；所有传输全程SSH加密；日志自动脱敏；插件沙箱隔离，无越权风险
3.  **兼容**：支持主流Linux发行版；兼容Ed25519、RSA等主流私钥格式；全桌面系统适配；插件API向下兼容
4.  **稳定性**：单连接双通道稳定运行2小时以上无中断；异常场景有降级处理，不崩溃；单个插件崩溃不影响主程序与其他功能
5.  **可扩展性**：核心模块与插件层完全解耦，API标准化，支持功能快速迭代与第三方生态拓展

## 十二、功能迭代优先级
### P0 MVP核心
1.  整体五区布局框架、左侧连接管理与工具菜单
2.  单SSH连接 + SFTP通道 + PTY终端通道
3.  双栏本地/远程文件浏览、基础文件操作
4.  底部终端基础交互、目录双向同步
5.  右侧服务器信息展示、基础传输队列

**验收标准**：连接稳定、文件浏览流畅、终端可用、核心操作无崩溃。

### P1 完善功能
1.  拖拽上传下载、完整传输队列管理
2.  惰性缓存文件编辑与冲突检测
3.  主机分组、目录记忆、布局状态持久化
4.  全局快捷键、右键菜单全功能覆盖
5.  右侧快捷操作全部功能落地

**验收标准**：文件编辑保存后1秒内自动回传；拖拽支持单文件最大1GB；断线30秒内自动重连；快捷键全场景生效。

### P2 专业增强
1.  端口转发、远程命令、密钥管理工具模块
2.  批量文件操作、权限批量修改
3.  多终端标签、终端分屏
4.  完整设置中心、首次启动引导
5.  插件基础运行时、插件中心基础框架、官方插件能力

**验收标准**：三种视图切换流畅；批量操作成功率≥99%；插件可正常安装卸载运行，不影响核心稳定性。

### P3 高级拓展与生态
1.  跳板机代理、多级跳转
2.  文件断点续传、传输限速
3.  全局文件搜索、内容搜索
4.  批量服务器管理、批量执行命令
5.  完整插件开放平台、第三方开发者接入、插件市场生态完善

**验收标准**：支持多级跳板机连接；断点续传文件MD5校验一致；插件API覆盖全部开放能力，第三方插件可正常上架运行。